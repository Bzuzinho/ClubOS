# Estado Vivo de Desenvolvimento — ClubOS

> Documento vivo para acompanhar a evolução funcional e técnica do ClubOS.
>
> Última atualização inicial: 2026-05-19.
>
> Fonte da análise inicial: leitura estática do repositório GitHub `Bzuzinho/ClubOS`, ramo `main`.

---

## 1. Objetivo deste documento

Este ficheiro deve funcionar como a fonte de verdade para acompanhar o desenvolvimento do ClubOS ao longo do tempo.

Sempre que existir um novo desenvolvimento, correção, refatoração ou integração, este documento deve ser atualizado com:

- funcionalidade afetada;
- subfuncionalidades concluídas;
- evidência técnica no código;
- percentagem estimada antes e depois;
- riscos ou pendências que continuam abertas.

A intenção é evitar perda de contexto entre conversas, prompts, commits, sprints e decisões técnicas.

---

## 2. Estado global estimado

| Área | Estado estimado |
|---|---:|
| Implementação global da aplicação | 60% a 65% |
| Maturidade técnica global | Média / avançada |
| Maturidade funcional global | Média |
| Maturidade para produção operacional plena | Ainda incompleta |

### Leitura geral

O ClubOS já não está numa fase meramente inicial. O repositório online apresenta uma aplicação Laravel + Inertia React com vários módulos reais: membros, financeiro, desportivo, eventos, portal, loja, logística, comunicação, configurações e patrocínios.

A aplicação já tem bastante estrutura, migrations, models, controllers, services, actions e páginas React/Inertia. Contudo, ainda existem sinais de duplicação de fluxos, sobretudo no financeiro, e áreas que parecem estruturalmente criadas mas ainda não totalmente consolidadas em processos fechados de ponta a ponta.

---

## 3. Stack técnica identificada

| Camada | Tecnologia |
|---|---|
| Backend | Laravel 11 |
| PHP | PHP 8.3 |
| Frontend | React 19 + TypeScript |
| Navegação | Inertia.js |
| Build | Vite |
| UI | Tailwind CSS, Radix UI, Headless UI, Lucide, Phosphor Icons |
| Dados / estado frontend | Axios, React Query |
| Gráficos | Recharts |
| Autenticação | Laravel auth, Sanctum, Breeze |
| Rotas frontend/backend | Laravel routes + Ziggy |
| Cache / performance | Redis/Predis, cache Laravel |
| Exportações | xlsx |

---

## 4. Grelha viva de funcionalidades

| Módulo / Área | Funcionalidades e subfuncionalidades | Integrações / Relações | Estado estimado | Observações |
|---|---|---|---:|---|
| Base técnica / arquitetura | Laravel, Inertia, React, Vite, Tailwind, Radix, Ziggy, Sanctum, Redis | Todos os módulos | 80% | Base sólida, mas convém reforçar organização de rotas e documentação técnica. |
| Autenticação e acessos | Login, middleware `auth`, `verified`, `module.access`, `permission.access`, tipos de utilizador | Configurações, Dashboard, Portal, módulos admin | 70% | Precisa de matriz formal de permissões por perfil. |
| Dashboard / entrada por perfil | Dashboard admin e vista atleta/portal; estatísticas, eventos recentes, receitas, atividade | Membros, Eventos, Financeiro, Desportivo, Família | 66% | Bem encaminhado; F3.2.1 restaurou linguagem operacional nas tabs de membro (`Conta Corrente`, `Mensalidades`, `Movimentos`, `Valor Pago`) mantendo `CurrentAccountService` como leitura canónica. Em F3.3, o payload operacional do Dashboard atleta deixou também de transportar `conta_corrente_manual`. Falta validação manual final de UX e indicadores. |
| Portal do atleta / utilizador | Perfil, treinos, eventos, pagamentos, resultados, documentos, comunicados, família, loja | Desportivo, Eventos, Financeiro, Documentos, Comunicação, Loja | 63% | Estrutura existe; F3.2.1 manteve Pagamentos e Família ligados a `CurrentAccountService`, mas retirou `Saldo manual legado` e a terminologia de dívida como linguagem principal, privilegiando `Conta Corrente`, `Em aberto` e `Crédito disponível`. Em F3.3, o Portal Profile deixou também de usar saldo manual legado como `Conta corrente`. Falta validação manual de experiência real em mobile. |
| Gestão de Membros / Pessoas | Lista, criação, edição, número de sócio, tipos, escalões, uploads, menor, EE/educando, dados financeiros, centros de custo | Financeiro, Desportivo, Configurações, Comunicação, Família | 85% | Sprint M2.6 validou operacionalmente o dual-write da M2.5 sem alterar lógica funcional: escrita da ficha e portal mantém destino principal em `dados_pessoais`/`dados_configuracao`, sincronização temporária de campos legacy em `users`, leitura via `MemberDataReadService` com fallback e auditoria sem regressão de conflitos no cenário validado. Em F1.0, `centrosCusto()`/`centro_custo_user` passou a ser a fonte operacional principal dos centros de custo do membro, com `users.centro_custo` apenas como fallback legacy auditável via `MemberCostCenterResolver` e comando `finance:audit-member-cost-centers`. Sem deploy em produção e sem remoção de campos legacy. Próxima sprint: M2.7 (rollout controlado no ambiente alvo e/ou redução gradual de dependências legacy, conforme decisão operacional). |
| Importação de membros | Template, preview e importação | Membros, user types, escalões | 60% | M4.2-F2 refatorou a escrita da importação para caminho canónico: `users` fica limitado a campos auth/operacionais, `dados_pessoais`/`dados_configuracao` passam a ser criados via `MemberDataWriteService`, `conta_corrente_manual` continua ignorado com warning e `tipo_mensalidade` continua a criar/atualizar `dados_financeiros`. |
| Documentos de membro | Gestão de documentos no admin e portal | Membros, Portal, RGPD, Configuração | 65% | Precisa de política clara de tipos obrigatórios e permissões. |
| Família / EE / educandos | Portal família, pesquisa e associação de membros familiares | Membros, Portal, Dashboard, Financeiro familiar | 60% | F3.2.1 passou a somar dívida aberta real por educando/família com `CurrentAccountService`, excluindo faturas futuras/ocultas, mantendo crédito disponível em separado e retirando saldo manual legado da superfície operacional. Falta validação manual orientada. |
| Desportivo | Dashboard, planeamento, treinos, presenças, cais, competições, relatórios | Eventos, Membros, Resultados, Financeiro, Configurações | 70% | Módulo bastante avançado, mas ainda precisa de validação funcional completa. |
| Planeamento desportivo | Épocas, macrociclos, mesociclos, microciclos, objetivos | Escalões, treinos, competições | 65% | Boa base; falta fechar UX e relatórios associados. |
| Treinos e presenças | Criar, agendar, editar, duplicar, apagar treino; atletas; presenças; cais; métricas | Eventos, atletas, portal | 70% | Núcleo forte; confirmar sincronização com eventos e ficha de atleta. |
| Competições / resultados | Competições, inscrições, resultados, splits, provas, resultados por equipa | Eventos, Desportivo, Relatórios | 63% | Estrutura existe e a XFIN3 removeu efeitos financeiros automáticos do model de inscrições: criação/cancelamento financeiro passou para actions transacionais explícitas com origem canónica `competition_registration`, sem `FinancialEntry` paralelo e com guardas de bloqueio em delete para estados parcial/pago, allocations confirmadas e emissão fiscal/recibos. |
| Eventos | CRUD, participantes, estados, estatísticas, portal de eventos | Membros, Comunicação, Desportivo, Financeiro | 65% | Falta validar fluxo completo de convocatórias/resultados/pagamentos. |
| Financeiro geral | Faturas, mensalidades, movimentos, extratos, banco, conciliação, relatórios, pedidos fiscais | Membros, Banco, Loja, Logística, Fiscal, Centros de custo | 89% | F2.5 acrescentou reabertura controlada de movimentos `pago/parcial` pelo endpoint canónico, com reversão segura de `Payment`, `PaymentAllocation`, `MapaConciliacao`, pedidos fiscais pendentes e estado do extrato bancário, mantendo bloqueado o update direto fora do fluxo próprio. O utilizador confirmou manualmente no browser a sequência testada dessa sprint. Em F3.1 passou a existir uma leitura canónica de dívida/conta corrente em `CurrentAccountService`, já usada por Dashboard atleta, Portal Profile e `financeiro.invoices.open`, com exclusão de faturas ocultas/futuras, uso de `valor_em_aberto`, crédito disponível explícito e separação do saldo manual legado. Em F3.2.1, Ficha do membro, DashboardTab/FinancialTab do membro, Portal Pagamentos e Portal Família mantiveram esta leitura canónica mas deixaram de promover `conta_corrente_manual`/`manual_account_balance` como saldo operacional, remetendo ajustes para movimentos manuais auditáveis no Financeiro e restaurando linguagem funcional nas superfícies. Em F3.3, criação/edição de membro e importação passaram a bloquear/ignorar `conta_corrente_manual`, o Portal Profile deixou de o mostrar como `Conta corrente` e o Dashboard atleta deixou de o transportar no payload operacional. Em F3.4, a auditoria foi validada operacionalmente no servidor real com `0` membros afetados, `0.00` total positivo, `0.00` total negativo e estado semântico `no_legacy_manual_balance_found`, concluindo que não existe legado real para migrar. Em F1.0, `centrosCusto()` tornou-se a fonte operacional principal dos centros de custo do membro e a geração de mensalidades passou a resolver `centro_custo_user` de forma canónica, com fallback legacy auditável e aviso de log quando necessário. Ajustes de conta corrente devem ser feitos por Movimentos manuais e não é necessário avançar para F3.5. |
| Faturas / mensalidades | Geração de mensalidades, faturas abertas, estados, itens, tipos de fatura | Membros, Dados financeiros, centros de custo, pagamentos | 83% | Faturas manuais respeitam o tipo escolhido, entram no modal canónico de pagamento via `faturasState`, já podem ser reabertas pelo endpoint canónico antes de existir documento fiscal externo e o update direto de reabertura continua bloqueado. O utilizador validou manualmente a regressão crítica de Mensalidades: `Dinheiro` sem banco liquida sem nº de recibo, `Transferência` sem banco bloqueia, `Transferência` com banco liquida/concilia e a desconciliação na tab Banco reabre a mensalidade e remove o pedido fiscal pendente. Em F3.1, o endpoint de faturas abertas passou também a excluir faturas ocultas/futuras e a devolver `valor_em_aberto` canónico mesmo com snapshots antigos. |
| Pagamentos e alocação canónica | `PaymentAllocationService`, criação de pagamentos, alocação a faturas, recalculo de estados, créditos, pedido fiscal automático | Faturas, banco, mapa de conciliação, pedidos fiscais | 87% | Transferência sem linha bancária bloqueia; transferência com linha bancária liquida/concilia; dinheiro sem linha bancária liquida pelo fluxo canónico; o modal frontend de Faturas só expõe métodos ativos e o popup de Movimentos passou a respeitar a mesma lista ativa e a mesma regra bancária, sem reintroduzir nº de recibo. A regressão manual de Mensalidades foi confirmada no browser, incluindo reabertura por desconciliação bancária com remoção do pedido fiscal pendente. |
| Conciliação bancária assistida | Sugestões, confirmação, rejeição, alocação de extrato, aliases bancários | Banco, faturas, pagamentos, mapa de conciliação | 88% | F4.1 (canonicidade e guardas de duplicados) foi validada manualmente no browser e fechada operacionalmente: criação de linha nova OK, bloqueio de duplicado individual OK, importação bulk com duplicados internos e duplicados já existentes em BD com resumo/mensagem visível OK, aceitação de linhas distintas com mesma data/valor mas referência/descrição diferente OK, conciliação/desconciliação de mensalidade e de despesa com reversões canónicas OK, fluxo legado/catalogar bloqueado OK e bloqueio de desconciliação após emissão Wintouch mantido OK. F4.2 reforçou o fluxo de sugestões com rejeições auditáveis, veto por assinatura rejeitada, alias mais conservador e UX de score/confiança mais explícita. F4.2.1 acrescentou sequência mensal cronológica por mês de referência (com cobertura total/parcial explícita, exclusão de faturas futuras/ocultas e proteção de confiança sem identidade segura). F4.2.2 corrigiu a prioridade para a sequência mensal sobre sugestões isoladas de valor exato, preservando o cap de confiança sem identidade e o contexto explicativo no metadata. F4.2.3 corrigiu a interpretação funcional para alocação assistida: a sugestão deixou de ser decisão fechada e passou a expor destinos elegíveis com distribuição manual entre faturas/mensalidades, movimentos e crédito, mantendo confirmação customizada sempre delegada ao fluxo canónico de liquidação; validação manual no browser concluída e sprint fechada operacionalmente. Em F4.4 foi criada e validada manualmente a camada de relatório/auditoria operacional da conciliação bancária em Configurações > Financeiro > Conciliação Bancária (subtab Auditoria), com endpoint paginado, filtros e detalhe de alocações sem alterar o motor financeiro. Em F4.5 foi adicionada exportação filtrada da auditoria (CSV detalhado e CSV de resumo operacional por período), com reaproveitamento dos mesmos filtros, limite de segurança de exportação e indicador leve de conferência de período; validada manualmente no browser e fechada operacionalmente. Em F4.6 foi feito hardening final sem novas funcionalidades: uniformização de linguagem operacional, confirmações explícitas para ações perigosas, microcopy, estados vazios e limpeza de detalhe técnico na UI; validada manualmente no browser em 2026-06-17 e fechada operacionalmente. Bloco F4 fechado. |
| Importação de recibos antigos | Batches, ZIP/diretoria pendente, matching, edição manual, commit, preview PDF | Faturas, banco, recibos PDF, utilizadores | 60% | A importação passou a validar corretamente ausência de ZIP quando a diretoria pendente não é usada. Em F4.1.4 a funcionalidade foi movida de Financeiro para Configurações > Financeiro > Importar Recibos, sem alterações em endpoints/controladores/serviços financeiros. Continua a faltar validação com PDFs reais e afinação operacional do commit/matching. |
| Emissão fiscal / Wintouch | Pedidos fiscais, estados operacionais, emitido, cancelado, erro de dados | Financeiro, faturas, recibos fiscais | 46% | O pedido fiscal continua pendente até tratamento manual e, quando marcado como emitido, retroalimenta `invoice.numero_recibo`. Falta fechar o fluxo completo Wintouch/manual e anulação fiscal na F6. |
| Logística / inventário | Stock, produtos, fornecedores, requisições, empréstimos, compras, movimentos de stock, faturação | Financeiro, Loja, Membros, Fornecedores | 69% | XFIN5 fechou o lifecycle financeiro de `LogisticsRequest` faturada: origem canónica `logistics_request`, guard financeiro para update/delete e resolução canónica de centro de custo via `MemberCostCenterResolver` (sem heurísticas por texto). Cobertura dedicada e auditoria pós-sprint sem findings em `logistics_requests`. |
| Loja | Loja pública, produto, carrinho, histórico, backoffice produtos/encomendas/hero | Membros, Financeiro, Logística, Configurações | 60% | Estrutura funcional; falta fechar ligação financeira e UX final. |
| Comunicação | Comunicados, campanhas, templates, segmentos, envios, alertas, lido/não lido | Membros, Eventos, Financeiro, Portal | 60% | Estrutura relevante; falta maturidade de entregas e automações. |
| Marketing | Campanhas de marketing | Comunicação, segmentos | 45% | Menos maduro e menos crítico para operação interna. |
| Configurações gerais | Clube, tipos, permissões, escalões, eventos, centros de custo, faturas, mensalidades, métodos de pagamento, artigos, categorias, patrocinadores, fornecedores, provas, notificações | Todos os módulos | 77% | A tab Financeiro gere métodos de pagamento ativos, ordem e exigência de linha bancária; validação manual OK. Em F4.1.4 passou também a expor a subtab `Importar Recibos` (relocalização de navegação), mantendo endpoints e regras de importação inalterados. |
| Configurações desportivas | Estados de atleta, tipos de treino, zonas, motivos de ausência, lesão, tipos de piscina | Desportivo, Membros, Presenças | 70% | Boa base; confirmar consumo real em formulários. |
| Patrocínios | CRUD, integrações, retry, fechar, cancelar | Financeiro, entidades, comunicação | 45% | Estrutura existe, mas parece menos consolidada. |
| Relatórios / dashboards | Relatórios financeiros, estatísticas desportivas, dashboard global | Financeiro, Desportivo, Membros, Eventos | 40% | Ainda disperso. Falta reporting consolidado real. |
| PWA / mobile | Manifest, favicon, ícones, rotas específicas para assets | Frontend, mobile, identidade clube | 55% | Base criada; validar instalação real iOS/Android. |
| Testes e qualidade | Testes financeiros identificados, nomeadamente alocação de pagamentos | Financeiro, regressões | 40% | A cobertura Financeiro inclui regressões explícitas para pagamento canónico de faturas `material`, `inscricao` e `mensalidade` sem nº de recibo, garante que a Emissão Fiscal continua a exigir `external_document_number` no `mark-issued`, tem contract test da tab Faturas, cobre `liquidarMovimento` sem nº de recibo e passou a cobrir a reabertura controlada de movimentos em dinheiro e por transferência, o bloqueio quando já existe documento fiscal emitido e a proibição de reabrir por `update` direto. Continua insuficiente face ao tamanho da aplicação. |

---

## 5. Principais riscos técnicos vivos

### 5.1. Duplicação da verdade financeira

Há vários modelos e fluxos financeiros coexistentes: `Payment`, `PaymentAllocation`, `Invoice`, `FinancialEntry`, `Movement`, `BankStatement`, `MapaConciliacao` e outros.

A regra recomendada é:

> Qualquer liquidação, conciliação, pagamento manual, pagamento bancário, importação de recibo, loja ou logística deve passar por um único serviço canónico de pagamento.

Neste momento, o candidato natural a serviço canónico é `PaymentAllocationService`.

Na leitura de dívida/conta corrente, `CurrentAccountService` passou a ser a fonte canónica para Dashboard atleta, Portal Profile, Ficha do membro, DashboardTab/FinancialTab do membro, Portal Pagamentos, Portal Família e `financeiro.invoices.open`. Mantém-se como risco vivo qualquer superfície administrativa fora deste conjunto que ainda apresente dívida fora desta leitura partilhada.

Mantém-se como risco apenas residual a existência de leituras de compatibilidade para saldo manual legado em `dados_financeiros.conta_corrente_manual` e no fallback legado `users.conta_corrente`, apesar de a validação operacional da F3.4 no servidor real não ter encontrado qualquer valor para migrar. O comando de migração continua bloqueado para `--commit` por desenho conservador e a regra operacional mantém-se: saldo manual legado não altera a conta corrente atual e ajustes de conta corrente devem ser feitos por Movimentos manuais.

### 5.2. Rotas demasiado concentradas em `routes/web.php`

O ficheiro `routes/web.php` concentra portal, membros, eventos, desportivo, financeiro, loja, logística, patrocínios, comunicação e configurações.

Recomendação futura:

- `routes/modules/financeiro.php`
- `routes/modules/desportivo.php`
- `routes/modules/membros.php`
- `routes/modules/portal.php`
- `routes/modules/logistica.php`
- `routes/modules/loja.php`
- `routes/modules/comunicacao.php`
- `routes/modules/configuracoes.php`

### 5.3. Normalização de dados de utilizador

O modelo `users` continua a concentrar muitos campos. Algumas áreas já estão separadas em tabelas especializadas, como dados financeiros, documentos, tipos, relações e centros de custo.

No fecho técnico da Sprint M1 (hardening operacional de Membros/Famílias/EE), ficou explícita a dívida estrutural que deve ser tratada numa sprint própria de consolidação de dados:

- `users` continua sobrecarregado com dados pessoais, configuração e metadados operacionais;
- as tabelas dedicadas `dados_pessoais` e `dados_configuracao` são lidas preferencialmente desde a Sprint M2.3 via `MemberDataReadService`, mas a escrita continua em `users` (sem dual-write);
- coexistem três superfícies de relação familiar (`user_guardian`, `user_relationships` e arrays legacy em `users`), o que aumenta o risco de inconsistência;
- a Sprint M2.3 fechou o cutover de leitura e a Sprint M2.4 fechou validação técnica/operacional sem cutover — próximo passo é M2.5 (cutover de escrita).

Recomendação explícita: abrir sprint estrutural dedicada para convergir dados de membro/família para um modelo único e reduzir acoplamento no `users`.

É importante manter a regra:

- identidade e autenticação em `users`;
- dados financeiros em tabelas financeiras;
- dados desportivos em tabelas desportivas;
- permissões/tipos em tabelas de configuração;
- documentos em tabela própria.

### 5.4. Testes insuficientes

A aplicação já tem testes em áreas críticas, mas o nível de cobertura deve aumentar.

Prioridade de testes:

1. liquidação de faturas;
2. conciliação bancária parcial;
3. importação de recibos antigos;
4. anulação/cancelamento de documentos fiscais;
5. permissões por perfil;
6. relações familiares;
7. geração de mensalidades;
8. sincronização treino/evento/presença.

---

## 6. Prioridades recomendadas

### Prioridade 1 — Fechar o financeiro canónico

Objetivo: garantir que todos os fluxos de pagamento usam o mesmo motor.

Tarefas:

- declarar `PaymentAllocationService` como caminho oficial;
- rever `FinanceiroController` e fluxos antigos;
- impedir escrita direta concorrente em estados de fatura;
- garantir que reaberturas de faturas pagas/parciais passam por endpoint canónico e nunca por `update` direto;
- garantir que importação de recibos, banco, mensalidades, loja e logística usam o mesmo caminho.

### Prioridade 2 — Fechar importação de recibos antigos

Objetivo: permitir arranque real do sistema com histórico já emitido.

Tarefas:

- testar PDFs reais;
- validar matching por atleta, NIF, número de sócio, mês e valor;
- permitir correção manual;
- suportar conciliação parcial de movimentos bancários;
- guardar alias para sugestões futuras.

### Prioridade 3 — Matriz de permissões

Objetivo: impedir acessos indevidos e clarificar a experiência por perfil.

Perfis mínimos:

- Administrador;
- Direção;
- Tesouraria;
- Treinador;
- Logística;
- Atleta;
- Encarregado de Educação.

### Prioridade 4 — Portal mobile / atleta / família

Objetivo: garantir que utilizadores normais entram sempre numa visualização simples, mobile-first e sem ruído administrativo.

Tarefas:

- validar dashboard atleta;
- validar família/educandos;
- validar pagamentos;
- validar treinos/eventos;
- validar documentos;
- validar botão de entrada em administração apenas para perfis autorizados.

### Prioridade 5 — Relatórios consolidados

Objetivo: transformar dados operacionais em indicadores úteis para gestão.

Relatórios prioritários:

- dívida por atleta/família;
- receitas por escalão;
- despesas por centro de custo;
- assiduidade por atleta/escalão;
- evolução de resultados;
- peso financeiro vs peso desportivo.

### XFIN1 — Auditoria transversal read-only das integracoes financeiras

Objetivo: mapear factual e transversalmente as integracoes financeiras legacy/paralelas entre Financeiro, Loja, Eventos, Desportivo, Logistica e Patrocinios, sem qualquer escrita corretiva.

Comando:

`php artisan finance:audit-integrations --json --report-path=storage/app/audits/xfin-financial-integrations.json`

Resumo factual esperado do contrato XFIN1:

- audita `competition_registrations`, `store`, `supplier_purchases`, `logistics_requests`, `convocation_groups`, `sponsorships` e `reporting`;
- emite findings normalizados com `severity`, `code`, `module`, `source_type`, `source_id`, `financial_record_type`, `financial_record_id`, `reason` e `metadata`;
- nao altera `invoices`, `movements`, `financial_entries`, hooks nem relatorios;
- suporta guards `--fail-on-critical` e `--fail-on-warning` para uso operacional de diagnostico.

Execucao factual no dataset alvo em 2026-07-08:

- total_findings=1
- critical=0
- warning=1
- info=0
- por modulo: `competition_registrations=0`, `store=0`, `supplier_purchases=0`, `logistics_requests=0`, `convocation_groups=0`, `sponsorships=0`, `reporting=1`
- top finding codes: `negative_expense_movement_value`
- guard `--fail-on-critical`: `EXIT_CODE=0`
- guard `--fail-on-warning`: `EXIT_CODE=1`

Decisao para sprint seguinte:

- usar o output factual da XFIN1 para priorizar correcoes de canonicidade por modulo sem misturar diagnostico com backfill ou alteracoes destrutivas.

### XFIN2 — Consolidacao de factos financeiros de reporting e convencao monetaria

Objetivo: convergir os relatorios e o dashboard para uma fonte unica de factos financeiros pagos, eliminando somas paralelas de `Invoice`/`FinancialEntry`/`Movement` e formalizando convencao monetaria (`amount` sempre positivo com natureza em `type=receita|despesa`).

Entregas principais:

- criado `FinancialReportingFactService` para gerar factos canonicos com contrato `source_kind`, `source_id`, `type`, `amount`, `paid_at`, `user_id`, `centro_custo_id`, `origem_modulo`, `origem_tipo`;
- inclusao de invoices pagas elegiveis independentemente do `tipo` (incluindo `mensalidade`, `inscricao`, `material` e outros tipos financeiros validos), sem canceladas/ocultas e sem `valor_pago` util;
- inclusao de `FinancialEntry` pagas sem duplicacao de `Invoice` ja selecionada por `fatura_id`;
- inclusao de `Movement` legacy apenas sem representacao canonica equivalente, com normalizacao `abs(valor_total)`;
- migracao de `FinanceReportService` e `FinanceDashboardService` para consumo da fonte unica de factos;
- ajuste da auditoria XFIN em `reporting` para validar selecao de invoice paga contra os factos canonicos em vez de regras legacy fragmentadas.

Regra de compatibilidade XFIN2:

- nao altera dados legados negativos nesta sprint;
- normaliza sinais em leitura (`abs`) e emite warning auditavel (`financial_reporting_negative_amount_normalized`) quando deteta montantes negativos.

### XFIN3 — Canonicalizacao do lifecycle financeiro de CompetitionRegistration

Objetivo: remover efeitos financeiros implícitos do model `CompetitionRegistration` e convergir inscrição/cancelamento para actions explícitas, transacionais e testadas.

Entregas principais:

- removido o hook financeiro automático no model `CompetitionRegistration` (sem criação implícita de `Invoice`, `InvoiceItem` e `FinancialEntry`);
- criada action `CreateCompetitionRegistrationAction` com cálculo de valor efetivo (`valor_inscricao` -> `evento.taxa_inscricao` -> `0`), criação condicional de dívida apenas quando valor > 0 e origem canónica `origem_tipo=competition_registration` + `origem_id=registration.id`;
- criada action `DeleteCompetitionRegistrationAction` com guardas funcionais para bloqueio de remoção em casos de parcial/pago, `valor_pago>0`, allocations confirmadas, documento fiscal emitido/external number e recibo emitido;
- criação de inscrição passou a gerar apenas `CompetitionRegistration + Invoice + InvoiceItem` (sem `FinancialEntry` paralelo), mantendo liquidação futura no fluxo canónico `Payment + PaymentAllocation`;
- controlador API de inscrições atualizado para usar as actions em `store` e `destroy`;
- auditoria XFIN mantida compatível com origem canónica e preservação de deteção legacy.

Execucao factual pos-XFIN3 no dataset alvo (2026-07-08):

- `total_findings=1`, `critical=0`, `warning=1`, `info=0`;
- modulo `competition_registrations=0` e warning remanescente em `reporting` (`negative_expense_movement_value`);
- guard `--fail-on-critical`: `EXIT_CODE=0`;
- guard `--fail-on-warning`: `EXIT_CODE=1`.

Regras desta entrega:

- sem backfill;
- sem alteracao de dados existentes legacy;
- sem alteracoes em `SupplierPurchase`, `LogisticsRequest`, `ConvocationGroup`, `Sponsorship`, sale legacy, reporting XFIN2 ou servicos canónicos de settlement/alocação.

### XFIN4 — Canonicalizacao da integracao financeira de SupplierPurchase

Objetivo: eliminar o fluxo paralelo `Movement + FinancialEntry` na criação/atualização/remoção de `SupplierPurchase`, convergindo para o contrato canónico `SupplierPurchase -> Movement -> FinancialSettlementService -> FinancialEntry(origem=movement)`.

Baseline factual pré-XFIN4 (read-only, 2026-07-08):

- total_supplier_purchases=1
- with_financial_movement_id=0
- with_financial_entry_id=0
- with_both_refs=0
- movements_origem_stock=0
- movements_origem_supplier_purchase=0
- entries_source_keyed_purchase_stock=0
- entries_origem_movement_for_purchase_movements=0
- negative_purchase_movements=0
- purchases_with_paid_finance_state=0
- purchases_with_confirmed_allocations=0
- purchases_reconciled=0
- purchases_with_fiscal_issued=0

Entregas principais XFIN4:

- `RegisterSupplierPurchaseAction` deixou de criar `FinancialEntry` direto e passou a criar apenas `SupplierPurchase`, `StockMovement`, `Movement` e `MovementItem`;
- `Movement` de compra passou a usar `origem_tipo=supplier_purchase`, `origem_id=purchase.id`, `classificacao=despesa` e `valor_total` positivo;
- `financial_movement_id` mantido como ligação operacional; `financial_entry_id` preservado apenas para leitura legacy (não preenchido em novas compras);
- criado `SupplierPurchaseFinancialGuardService` para bloquear mutação/remoção quando existir impacto financeiro (estado pago/parcial, allocations/pagamentos confirmados, conciliação, fiscal emitido e legado paralelo ambíguo);
- `UpdateSupplierPurchaseAction` refatorada para atualizar apenas compra/itens/stock/stock movements/movement/movement items, reutilizando o mesmo `Movement` e sem tocar em `FinancialEntry`;
- `DeleteSupplierPurchaseAction` refatorada para bloquear cenários com risco financeiro e remover apenas registos operacionais pendentes (sem apagar `FinancialEntry` diretamente);
- auditoria XFIN de `supplier_purchases` atualizada para reconhecer origem canónica `supplier_purchase` e manter findings legacy (`supplier_purchase_parallel_movement_and_entry`, `supplier_purchase_source_keyed_entry`, `supplier_purchase_orphan_financial_movement_reference`, `supplier_purchase_orphan_financial_entry_reference`, `supplier_purchase_multiple_movements`, `supplier_purchase_multiple_entries`, `negative_expense_movement_value`);
- cobertura nova dedicada em `SupplierPurchaseFinancialLifecycleTest` (create/settlement/update/delete + guardas de bloqueio + cenário legacy conservador).

Execucao factual pós-XFIN4 (2026-07-08):

- `total_findings=1`, `critical=0`, `warning=1`, `info=0`;
- modulo `supplier_purchases=0` (sem findings novos no fluxo XFIN4);
- warning remanescente em `reporting`: `negative_expense_movement_value` (legado fora do escopo da sprint);
- guard `--fail-on-critical`: `EXIT_CODE=0`;
- guard `--fail-on-warning`: `EXIT_CODE=1`.

Decisão XFIN4.1:

- nao abrir backfill destrutivo nesta sprint;
- manter proposta XFIN4.1 apenas se o ambiente alvo evidenciar legado real de `SupplierPurchase` com paralelismo/órfãos em runtime.

### XFIN5 — Protecao do lifecycle financeiro de LogisticsRequest faturada

Objetivo: impedir mutações destrutivas em `LogisticsRequest` com impacto financeiro real e convergir faturação para contrato canónico (`origem_tipo=logistics_request`) com resolução canónica de centro de custo.

Baseline factual pré-XFIN5 (read-only, 2026-07-08):

- `finance:audit-integrations` com `total_findings=1`, `critical=0`, `warning=1` (apenas `negative_expense_movement_value` no módulo `reporting`);
- módulo `logistics_requests=0` no baseline pré-sprint;
- dataset alvo sem legado ativo de lifecycle de `LogisticsRequest` (sem findings prévios de órfãos/paralelismo).

Entregas principais XFIN5:

- criado `LogisticsRequestFinancialGuardService` para bloquear update/delete quando existir invoice parcial/paga, `valor_pago>0`, allocations/pagamentos confirmados, conciliação confirmada, emissão fiscal/documento externo ou sinais de recibo associado;
- criado `LogisticsRequestCostCenterResolver` para usar `MemberCostCenterResolver` com regra determinística: centro único/top-único -> usa; empate/ambiguidade -> `null`;
- `InvoiceLogisticsRequestAction` passou a usar origem canónica `logistics_request` e resolver partilhado de centro de custo;
- `UpdateLogisticsRequestAction` passou a executar guard antes de mutar requisição faturada e a sincronizar invoice/item no mesmo registo, recalculando snapshot financeiro (`valor_pago=0`, `valor_em_aberto=valor_total`, estado `pendente|vencido`, limpeza de campos de pagamento);
- `DeleteLogisticsRequestAction` passou a executar guard financeiro e, quando permitido, limpa apenas pedidos fiscais pendentes (`deletePendingForInvoice`) antes de apagar invoice, sem remover pagamentos/alocações/conciliações/fiscais emitidos;
- `FinancialIntegrationAuditService` no módulo `logistics_requests` foi alinhado para contrato XFIN5 com códigos explícitos (`logistics_request_orphan_invoice_reference`, `logistics_request_missing_invoice`, `logistics_invoice_orphan_source`, `logistics_invoice_total_mismatch`, `logistics_invoice_user_mismatch`, `logistics_paid_invoice_mutable_lifecycle`, `logistics_allocated_invoice_mutable_lifecycle`, `logistics_fiscal_invoice_mutable_lifecycle`);
- cobertura nova dedicada em `LogisticsRequestFinancialLifecycleTest` (bloqueios por pago/allocation/fiscal/conciliação, delete permitido pendente, centro de custo canónico e empate) e atualização de contratos em `LogisticaFlowsTest` e `FinancialIntegrationAuditCommandTest`.

Execucao factual pós-XFIN5 (2026-07-08):

- `total_findings=1`, `critical=0`, `warning=1`, `info=0`;
- módulo `logistics_requests=0` (sem findings após hardening XFIN5);
- warning remanescente fora de escopo no módulo `reporting`: `negative_expense_movement_value`;
- guard `--fail-on-critical`: `EXIT_CODE=0`;
- guard `--fail-on-warning`: `EXIT_CODE=1`.

Decisão XFIN5.1:

- não executar backfill nesta sprint;
- manter correção estritamente de runtime/guard rails/auditoria/testes;
- legado de `negative_expense_movement_value` segue para triagem em sprint específica de reporting.

---

## 7. Histórico vivo de atualizações

| Data | Módulo | Desenvolvimento / análise | Evidência | Percentagem antes | Percentagem depois | Pendências |
|---|---|---|---|---:|---:|---|
| 2026-07-08 | Logística / Financeiro transversal | Sprint XFIN5 concluída para proteger o lifecycle financeiro de `LogisticsRequest` faturada. Foi criado guard financeiro dedicado para bloquear update/delete quando existir pagamento/alocação/conciliação/fiscal/recibo associado, a faturação da requisição convergiu para origem canónica `origem_tipo=logistics_request`, e a resolução de centro de custo passou a usar caminho canónico via `MemberCostCenterResolver` com regra determinística (top-único) e fallback seguro para `null` em ambiguidade. O update de requisição faturada passou a sincronizar a mesma invoice e itens com recomputação de snapshot financeiro pendente, e o delete permitido passou a limpar apenas pedidos fiscais pendentes antes de apagar invoice. Auditoria XFIN pós-sprint manteve `logistics_requests=0` e sem críticos globais. | `app/Services/Logistica/LogisticsRequestFinancialGuardService.php`, `app/Services/Logistica/LogisticsRequestCostCenterResolver.php`, `app/Services/Logistica/InvoiceLogisticsRequestAction.php`, `app/Services/Logistica/UpdateLogisticsRequestAction.php`, `app/Services/Logistica/DeleteLogisticsRequestAction.php`, `app/Services/Financeiro/FinancialIntegrationAuditService.php`, `tests/Feature/Logistica/LogisticsRequestFinancialLifecycleTest.php`, `tests/Feature/Logistica/LogisticaFlowsTest.php`, `tests/Feature/Financeiro/FinancialIntegrationAuditCommandTest.php`, `storage/app/audits/xfin-financial-integrations-post-xfin5.json`, `php artisan test --filter=LogisticsRequestFinancialLifecycleTest`, `php artisan test --filter=FinancialIntegrationAuditCommandTest`, `php artisan test --filter=FinanceReportFlowTest`, `php artisan test --filter=CurrentAccountServiceOperationalBalanceTest`, `php artisan test --filter=LogisticaFlowsTest`, `php artisan finance:audit-integrations --json --report-path=storage/app/audits/xfin-financial-integrations-post-xfin5.json`, `php artisan finance:audit-integrations --fail-on-critical`, `php artisan finance:audit-integrations --fail-on-warning` | 69% | 72% | Warning global remanescente (`negative_expense_movement_value`) permanece fora de escopo de XFIN5; sem backfill por decisão conservadora. |
| 2026-07-08 | Logística / Financeiro transversal | Sprint XFIN4 concluída para canonicalizar a integração financeira de `SupplierPurchase`: criação passou a gerar apenas `SupplierPurchase + Movement + MovementItems + StockMovement` (sem `FinancialEntry` paralelo), `Movement` agora usa `origem_tipo=supplier_purchase` com `valor_total` positivo, `Update/Delete` foram protegidos por guard financeiro reutilizável (`SupplierPurchaseFinancialGuardService`) e deixaram de mutar/destruir `FinancialEntry` diretamente. Auditoria XFIN para `supplier_purchases` foi alinhada ao contrato canónico e a suíte nova `SupplierPurchaseFinancialLifecycleTest` cobre create, settlement, update/delete pré-settlement e bloqueios por pagamento/allocation/conciliação/fiscal/legado. Resultado pós-XFIN4 manteve apenas warning global legacy `negative_expense_movement_value` no módulo `reporting`, sem findings em `supplier_purchases`. | `app/Services/Logistica/RegisterSupplierPurchaseAction.php`, `app/Services/Logistica/UpdateSupplierPurchaseAction.php`, `app/Services/Logistica/DeleteSupplierPurchaseAction.php`, `app/Services/Logistica/SupplierPurchaseFinancialGuardService.php`, `app/Models/SupplierPurchase.php`, `app/Services/Financeiro/FinancialIntegrationAuditService.php`, `tests/Feature/Logistica/SupplierPurchaseFinancialLifecycleTest.php`, `tests/Feature/Logistica/LogisticaFlowsTest.php`, `tests/Feature/Financeiro/FinancialIntegrationAuditCommandTest.php`, `storage/app/audits/xfin-financial-integrations-pre-xfin4.json`, `storage/app/audits/xfin-financial-integrations-post-xfin4.json`, `php artisan test --filter=SupplierPurchaseFinancialLifecycleTest`, `php artisan test --filter=FinancialIntegrationAuditCommandTest`, `php artisan test --filter=FinanceReportFlowTest`, `php artisan test --filter=FinanceDashboardFlowTest`, `php artisan test --filter=LogisticaFlowsTest`, `php artisan finance:audit-integrations --json --report-path=storage/app/audits/xfin-financial-integrations-post-xfin4.json`, `php artisan finance:audit-integrations --fail-on-critical`, `php artisan finance:audit-integrations --fail-on-warning` | 49% | 50% | Sem backfill e sem alteração destrutiva de legado nesta sprint. Reavaliar necessidade de XFIN4.1 apenas se o ambiente alvo revelar `SupplierPurchase` legacy com paralelismo/órfãos. |
| 2026-07-08 | Desportivo / Financeiro transversal | Sprint XFIN3 concluída para canonicalizar o lifecycle financeiro de `CompetitionRegistration`. Efeitos financeiros automáticos foram removidos do model e substituídos por actions explícitas/transacionais de criação e remoção segura (`CreateCompetitionRegistrationAction`, `DeleteCompetitionRegistrationAction`). A criação agora calcula valor efetivo por prioridade (`valor_inscricao` -> `evento.taxa_inscricao` -> `0`), cria `Invoice` e `InvoiceItem` apenas para valor > 0, associa `fatura_id`, define origem canónica `competition_registration` e elimina `FinancialEntry` paralelo. O destroy passou a bloquear casos com parcial/pago, `valor_pago>0`, allocations confirmadas, emissão fiscal externa e recibo emitido, permitindo remoção apenas em estados pendentes/cancelados sem vínculos financeiros ativos. Cobertura nova dedicada em `CompetitionRegistrationFinancialLifecycleTest` (cenários A-K) e regressões XFIN2 executadas sem falhas. Auditoria pós-XFIN3 manteve `total_findings=1` (`negative_expense_movement_value`), com `--fail-on-critical=0` e `--fail-on-warning=1`. | `app/Models/CompetitionRegistration.php`, `app/Http/Controllers/Api/CompetitionRegistrationController.php`, `app/Services/Desportivo/CreateCompetitionRegistrationAction.php`, `app/Services/Desportivo/DeleteCompetitionRegistrationAction.php`, `app/Services/Financeiro/FinancialIntegrationAuditService.php`, `tests/Feature/Sports/CompetitionRegistrationFinancialLifecycleTest.php`, `tests/Feature/Financeiro/FinancialIntegrationAuditCommandTest.php`, `storage/app/audits/xfin-financial-integrations-post-xfin3.json`, `php artisan test --filter=CompetitionRegistrationFinancialLifecycleTest`, `php artisan test --filter=SportsCanonicalFlowTest`, `php artisan test --filter=FinancialIntegrationAuditCommandTest`, `php artisan test --filter=FinanceReportFlowTest`, `php artisan test --filter=CurrentAccountServiceOperationalBalanceTest`, `php artisan finance:audit-integrations --json --report-path=storage/app/audits/xfin-financial-integrations-post-xfin3.json`, `php artisan finance:audit-integrations --fail-on-critical`, `php artisan finance:audit-integrations --fail-on-warning` | 49% | 50% | Manter warning legacy `negative_expense_movement_value` para sprint dedicada de contrato/sinal em despesas; continuar canonicidade dos lifecycles restantes (SupplierPurchase, LogisticsRequest, ConvocationGroup, Sponsorship) sem backfill destrutivo nesta frente. |
| 2026-07-08 | Financeiro / Reporting transversal | Sprint XFIN2 concluida para consolidar reporting financeiro transversal e convencao monetaria. Criado `FinancialReportingFactService` como fonte canonica de factos pagos (`source_kind/source_id/type/amount/paid_at/user_id/centro_custo_id/origem_modulo/origem_tipo`) com normalizacao de sinais (`abs`) e warning auditavel para legados negativos. `FinanceReportService` e `FinanceDashboardService` migrados para uma unica agregacao de receitas/despesas (saldo = receitas - despesas), removendo somas independentes por origem. Invoices pagas nao-mensalidade passaram a entrar por defeito no reporting canónico; deduplicacao aplicada para `Invoice + FinancialEntry(fatura_id)` e para `Movement` com representacao canonica (`origem=movement` ou origem funcional ja coberta). Auditoria XFIN em `reporting` alinhada com factos canónicos, eliminando falso positivo de exclusao de invoice paga. Cobertura de testes expandida com `FinancialReportingFactServiceTest` e atualizacao dos fluxos `FinanceDashboardFlowTest`, `FinanceReportFlowTest` e `FinancialIntegrationAuditCommandTest`. | `app/Services/Financeiro/FinancialReportingFactService.php`, `app/Services/Financeiro/FinanceReportService.php`, `app/Services/Financeiro/FinanceDashboardService.php`, `app/Services/Financeiro/FinancialIntegrationAuditService.php`, `tests/Feature/Financeiro/FinancialReportingFactServiceTest.php`, `tests/Feature/Financeiro/FinanceDashboardFlowTest.php`, `tests/Feature/Financeiro/FinanceReportFlowTest.php`, `tests/Feature/Financeiro/FinancialIntegrationAuditCommandTest.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 47% | 49% | Reexecutar auditoria XFIN1 no dataset alvo e confirmar `paid_invoice_excluded_from_financial_reports=0`; manter monitorizacao do risco estrutural `financial_report_double_count_risk` ate fecho de lifecycles fora de escopo (CompetitionRegistration, SupplierPurchase, LogisticsRequest, ConvocationGroup, Sponsorship). |
| 2026-07-08 | Financeiro / Loja / Eventos / Desportivo / Logística / Patrocínios | Sprint XFIN1 concluída em modo estritamente read-only para auditar integrações financeiras transversais. Entregues `FinancialIntegrationAuditService` e comando `finance:audit-integrations` com `--json`, `--report-path`, `--module`, `--fail-on-critical` e `--fail-on-warning`. A auditoria cruza CompetitionRegistration, Loja legacy/LojaEncomenda, SupplierPurchase, LogisticsRequest, ConvocationGroup, Sponsorship* e FinanceReportQueryService para classificar origens ambíguas, paralelismo invoice/entry, paralelismo movement/entry, referências órfãs, riscos de lifecycle destrutivo, exclusões de reporting, double count risk e sinais negativos inconsistentes. Criado contrato documental XFIN com os dois caminhos financeiros esperados: `Business Source -> Invoice -> Payment -> PaymentAllocation` e `Business Source -> Movement -> FinancialSettlementService -> FinancialEntry origem=movement -> PaymentAllocation`. Execução factual no dataset alvo: `total_findings=1`, `critical=0`, `warning=1`, `info=0`; por módulo `competition_registrations=0`, `store=0`, `supplier_purchases=0`, `logistics_requests=0`, `convocation_groups=0`, `sponsorships=0`, `reporting=1`; top code `negative_expense_movement_value`; guards `--fail-on-critical=0` e `--fail-on-warning=1`. | `app/Services/Financeiro/FinancialIntegrationAuditService.php`, `app/Console/Commands/Financeiro/FinancialIntegrationAuditCommand.php`, `bootstrap/app.php`, `tests/Feature/Financeiro/FinancialIntegrationAuditCommandTest.php`, `docs/XFIN_CONTRATO_INTEGRACOES_FINANCEIRAS.md`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md`, `storage/app/audits/xfin-financial-integrations.json`, `php artisan test --filter=FinancialIntegrationAuditCommandTest`, `php artisan finance:audit-integrations --json --report-path=storage/app/audits/xfin-financial-integrations.json`, `php artisan finance:audit-integrations --fail-on-critical`, `php artisan finance:audit-integrations --fail-on-warning` | 90% | 90% | Próxima sprint deve decidir se `Movement.classificacao=despesa` continuará a usar sinal negativo ou se a normalização de sinais será consolidada no contrato canónico antes de qualquer correção de dados/histórico. |
| 2026-07-07 | Membros / Financeiro | Sprint F3 concluida para consolidar a conta corrente operacional canónica do membro com `CurrentAccountService` como fonte única de saldo apresentado. Criado `MemberManualAccountBalanceResolver` para resolver exclusivamente o ajuste manual (`dados_financeiros.conta_corrente_manual`) com fallback legacy temporário auditável em `users.conta_corrente` apenas quando aplicável; fallback passou a emitir warning `current_account_legacy_fallback` com contexto mínimo sem dados pessoais. Integrada a resolução manual no `CurrentAccountService` sem alterar responsabilidades de agregação de dívida/crédito e mantendo semântica explícita de `manual_account_balance` como componente separada do cálculo. Criado comando read-only `finance:audit-member-current-accounts` com `--json`, `--report-path`, `--user`, `--fail-on-divergence` e `--fail-on-fallback`, incluindo dupla classificação (`manual_source_classification`, `operational_balance_classification`) e agregados diagnósticos. Superfícies de membro/dashboard foram endurecidas para usar saldo operacional do summary sem fallback direto de `user.conta_corrente`. | `app/Services/Financeiro/MemberManualAccountBalanceResolver.php`, `app/Services/Financeiro/CurrentAccountService.php`, `app/Console/Commands/Financeiro/AuditMemberCurrentAccountsCommand.php`, `bootstrap/app.php`, `resources/js/Components/Members/Tabs/DashboardTab.tsx`, `resources/js/Components/Members/Tabs/FinancialTab.tsx`, `resources/js/Pages/Dashboard/Atleta.tsx`, `tests/Feature/Financeiro/MemberManualAccountBalanceResolverTest.php`, `tests/Feature/Financeiro/AuditMemberCurrentAccountsCommandTest.php`, `tests/Feature/Financeiro/CurrentAccountServiceOperationalBalanceTest.php`, `tests/Feature/Membros/MembrosCurrentAccountSurfaceTest.php`, `tests/Feature/Portal/PortalFamilyCurrentAccountTest.php`, `storage/app/audits/member-current-accounts-audit.json`, `php artisan finance:audit-member-current-accounts --json --report-path=storage/app/audits/member-current-accounts-audit.json`, `php artisan finance:audit-member-current-accounts --fail-on-divergence`, `php artisan finance:audit-member-current-accounts --fail-on-fallback` | 91% | 92% | Resultado factual no dataset alvo: `total_users=79`, `canonical_manual_only=78`, `matching_manual=0`, `legacy_fallback=0`, `divergent_manual=0`, `no_manual_adjustment=1`, `invalid_value=0`, `operational_debt=4`, `operational_credit=0`, `operational_balanced=75`, `gross_debt_total=406.00`, `available_credit_total=0.00`, `net_debt_total=406.00`; guards `--fail-on-divergence` e `--fail-on-fallback` com `EXIT_CODE=0`. Sem backfill/escrita corretiva nesta sprint (sem cópia de `users.conta_corrente`, sem alterações a `dados_financeiros.conta_corrente_manual`, sem alterações a invoices/movements/payments/credits). Decisão: F3.1/F3.2 podem focar apenas em limpeza faseada de compatibilidade legacy após validação operacional continuada, sem urgência por fallback no dataset atual. |
| 2026-07-08 | Membros / Financeiro | Sprint FC2 concluída end-to-end com cleanup físico dos campos financeiros legacy em `users` (`centro_custo`, `tipo_mensalidade`, `conta_corrente`) via migration dedicada, endurecimento sqlite (`DROP INDEX IF EXISTS users_tipo_mensalidade_index` antes do drop), adaptação runtime para modo pós-drop (queries/seleções schema-safe) e atualização da bateria de testes/contratos para semântica canónica (`dados_financeiros` + `centro_custo_user`). Comando de readiness mantém bloqueio semântico para leituras/escritas proibidas e marca `cleanup_completed` quando a coluna já não existe. | `database/migrations/2026_07_08_120000_drop_users_financial_legacy_fields_fc2.php`, `app/Models/User.php`, `app/Services/Financeiro/FinanceLegacyCleanupReadinessAuditor.php`, `app/Services/Financeiro/MemberCostCenterLegacyBackfillService.php`, `app/Services/Financeiro/MemberMonthlyFeeLegacyBackfillService.php`, `app/Services/Financeiro/MonthlyFeeGenerationService.php`, `app/Services/Financeiro/CurrentAccountService.php`, `app/Services/Members/MemberImportService.php`, `app/Http/Controllers/FinanceiroController.php`, `app/Services/Financeiro/PendingMonthlyFeeRequirementAuditor.php`, `config/member_user_legacy_fields.php`, `tests/Feature/Financeiro/UsersFinancialLegacyPhysicalCleanupSchemaTest.php`, `tests/Feature/Financeiro/FinanceLegacyCleanupReadinessAuditTest.php`, `tests/Feature/Financeiro/BackfillMemberMonthlyFeesCommandTest.php`, `tests/Feature/Financeiro/MemberCostCenterLegacyBackfillCommandTest.php`; validações: `composer dump-autoload`, `php artisan migrate --pretend`, auditorias `finance:audit-*` (exit 0), `php artisan test` (bateria FC2), `npm run build`. | 89% | 90% | Validar em ambiente alvo (dataset real) os relatórios de auditoria pós-drop e confirmar que não existem jobs/scripts externos ainda a depender dos campos removidos em `users`. |
| 2026-07-08 | Membros / Financeiro | Sprint FC1.1 concluída para eliminar dependências operacionais dos campos financeiros legacy em `users` (`centro_custo`, `tipo_mensalidade`, `conta_corrente`) sem backfill, sem remoção de schema e sem escrita corretiva. Removidos fallback/dual-write dos caminhos canónicos em `MemberCostCenterResolver`, `MemberMonthlyFeeResolver`, `MemberManualAccountBalanceResolver`, `MonthlyFeeGenerationService`, `MembrosController`, `FinanceiroController`, `PortalPageController` e `PortalProfileController`; auditor FC1 refinada para classificar `audit_or_migration`, `compatibility_dual_write`, `test_fixture`, `documentation` e `false_positive`, fechando a triagem com `ready_fields_count=3`, `not_ready_fields_count=0`, `prohibited_read_findings_count=0`, `prohibited_write_findings_count=0` e `unknown_findings_count=0`. Guards funcionais validados com `finance:audit-member-cost-centers --fail-on-divergence/--fail-on-fallback`, `finance:audit-member-monthly-fees --fail-on-divergence/--fail-on-fallback` e `finance:audit-member-current-accounts --fail-on-divergence/--fail-on-fallback`, todos com `EXIT_CODE=0`. | `app/Services/Financeiro/FinanceLegacyCleanupReadinessAuditor.php`, `app/Services/Financeiro/MemberCostCenterResolver.php`, `app/Services/Financeiro/MemberMonthlyFeeResolver.php`, `app/Services/Financeiro/MemberManualAccountBalanceResolver.php`, `app/Services/Financeiro/MonthlyFeeGenerationService.php`, `app/Http/Controllers/MembrosController.php`, `app/Http/Controllers/FinanceiroController.php`, `app/Http/Controllers/PortalPageController.php`, `app/Http/Controllers/PortalProfileController.php`, `tests/Feature/Financeiro/FinanceLegacyCleanupReadinessAuditTest.php`, `tests/Feature/Financeiro/MemberCostCenterAuditFlowTest.php`, `tests/Feature/Financeiro/AuditMemberCurrentAccountsCommandTest.php`, `tests/Feature/Financeiro/MemberMonthlyFeeSyncServiceTest.php`, `tests/Feature/Financeiro/MemberManualAccountBalanceResolverTest.php`, `storage/app/audits/finance-legacy-cleanup-readiness-post-code-cleanup.json`, `php artisan finance:audit-legacy-cleanup-readiness --json --report-path=storage/app/audits/finance-legacy-cleanup-readiness-post-code-cleanup.json`, `php artisan test --filter='AuditMemberCurrentAccountsCommandTest|MemberCostCenterAuditFlowTest'`, `php artisan finance:audit-member-cost-centers --fail-on-divergence`, `php artisan finance:audit-member-cost-centers --fail-on-fallback`, `php artisan finance:audit-member-monthly-fees --fail-on-divergence`, `php artisan finance:audit-member-monthly-fees --fail-on-fallback`, `php artisan finance:audit-member-current-accounts --fail-on-divergence`, `php artisan finance:audit-member-current-accounts --fail-on-fallback` | 85% | 86% | Sem pendência funcional nesta sprint; cleanup físico e backfill continuam fora de escopo por desenho. |
| 2026-07-07 | Membros / Financeiro | Sprint F2.3 concluida com correcao canonica da obrigacao de plano de mensalidade. A causa raiz foi formalizada: `estado=ativo` isolado estava a marcar obrigacao de mensalidade de forma demasiado ampla. Criado `MemberMonthlyFeeEligibilityService` como fonte unica de decisao (`shouldHaveMonthlyFee`/`evaluate`) com reutilizacao obrigatoria de `MemberTypeResolver`; regra inicial explicita: apenas atleta com tipo funcional resolvido + `estado=ativo` + `ativo_desportivo=true` e obrigatorio. Integrada a elegibilidade canonica em `AuditMemberMonthlyFeesCommand`, `PendingMonthlyFeeRequirementAuditor` e `MonthlyFeeGenerationService`, removendo decisao paralela por estado ativo isolado. O caso diagnosticado (`user_id=a1284fe5-d152-4701-8b68-6ebbd28496de`) foi reclassificado de `missing_required` para `not_required` com `reason_codes=[missing_operational_type,no_monthly_fee_eligible_member_type]`, sem qualquer escrita corretiva no membro. Resultado factual pos-correcao no dataset alvo: `total_users=79`, `matching=78`, `legacy_fallback=0`, `divergent=0`, `invalid_reference=0`, `missing_required=0`, `not_required=1`; guard rails `--fail-on-divergence` e `--fail-on-fallback` com `EXIT_CODE=0`; auditoria F2.2 (`finance:audit-pending-monthly-fee-requirements --json`) com `total_cases=0`. | `app/Services/Financeiro/MemberMonthlyFeeEligibilityService.php`, `app/Console/Commands/Financeiro/AuditMemberMonthlyFeesCommand.php`, `app/Services/Financeiro/PendingMonthlyFeeRequirementAuditor.php`, `app/Services/Financeiro/MonthlyFeeGenerationService.php`, `config/clubos.php`, `tests/Feature/Financeiro/MemberMonthlyFeeEligibilityServiceTest.php`, `tests/Feature/Financeiro/MemberMonthlyFeeAuditCommandTest.php`, `tests/Feature/Financeiro/PendingMonthlyFeeRequirementAuditCommandTest.php`, `tests/Feature/Financeiro/MonthlyFeeGenerationFlowTest.php`, `storage/app/audits/member-monthly-fees-post-eligibility-fix.json`, `php artisan finance:audit-member-monthly-fees --fail-on-divergence`, `php artisan finance:audit-member-monthly-fees --fail-on-fallback`, `php artisan finance:audit-pending-monthly-fee-requirements --json` | 90% | 91% | Sem auto-atribuicao de mensalidade e sem alteracao de dados do membro diagnosticado (`users`, `dados_financeiros`, `invoices`, `member_types` inalterados). Manter regra explicita para outros tipos apenas quando existir decisao funcional formal no dominio. |
| 2026-07-07 | Membros / Financeiro | Sprint F2.2 concluída em modo estritamente read-only para diagnóstico funcional dos casos `missing_required` da auditoria F2. Entregues `PendingMonthlyFeeRequirementAuditor` e comando `finance:audit-pending-monthly-fee-requirements` (`--json`, `--report-path`, `--user`) reutilizando `MemberMonthlyFeeResolver`, `MemberCostCenterResolver` e `MemberTypeResolver`, sem inferir plano e sem alterar critérios operacionais da obrigação (`estado=ativo`). Cobertura de testes adicionada para 6 classificações, contrato JSON, `--report-path`, `--user` e garantia read-only. Execução no dataset alvo: `total_cases=1`, classificação final `manual_review_required`, `reason_codes=[manual_review_required,no_operational_member_type,no_monthly_invoice_history]`; condição funcional factual: `perfil=admin`, `estado=ativo`, sem tipos funcionais (`member_types=[]`), sem `dados_financeiros`, sem plano canónico/legacy, sem centros de custo, sem histórico de invoices de mensalidade. Critério que causou `should_have_monthly_fee=true`: regra operacional vigente `estado_ativo` (matched=true) com `eligibility.reason_codes=[member_state_active_requires_monthly_fee,no_operational_member_type]`. Recomendação objetiva: escalar para decisão funcional manual sem auto-atribuição de mensalidade nesta sprint. Confirmado impacto de escrita: zero (`users`, `dados_financeiros`, `invoices` e geração de mensalidades inalterados por desenho e por teste read-only). | `app/Services/Financeiro/PendingMonthlyFeeRequirementAuditor.php`, `app/Console/Commands/Financeiro/AuditPendingMonthlyFeeRequirementsCommand.php`, `bootstrap/app.php`, `tests/Feature/Financeiro/PendingMonthlyFeeRequirementAuditCommandTest.php`, `storage/app/audits/pending-monthly-fee-requirements.json`, `php artisan test --filter=PendingMonthlyFeeRequirementAuditCommandTest`, `php artisan finance:audit-pending-monthly-fee-requirements --json --report-path=storage/app/audits/pending-monthly-fee-requirements.json` | 90% | 90% | Não corrigir nesta sprint: sem atribuição de plano, sem escrita em `dados_financeiros`, sem alteração em `users.tipo_mensalidade`, sem geração de invoices/mensalidades. Próxima decisão deve validar funcionalmente se a regra de obrigatoriedade por `estado=ativo` permanece adequada para perfis administrativos sem tipo operacional. |
| 2026-07-07 | Membros / Financeiro | Sprint F2.1 concluída com backfill conservador de mensalidades legacy-only para fonte canónica `dados_financeiros.mensalidade_id`, reutilizando `MemberMonthlyFeeResolver` (classificação) e `MemberMonthlyFeeSyncService` (write path único, dual-write temporário preservado). Entregues `MemberMonthlyFeeLegacyBackfillService` e comando `finance:backfill-member-monthly-fees` com `--apply`, `--json`, `--report-path`, `--fail-on-skipped` e `--user`. Preflight factual no dataset alvo: `total=79`, `ready_for_backfill=43`, `already_canonical=35`, `divergent=0`, `invalid_legacy_reference=0`, `missing_required=1`, `can_apply=true`, sem bloqueios; validação cruzada confirmou `43/43` casos `legacy_fallback` da auditoria F2 classificados como `ready_for_backfill`. Apply factual executado com `migrated=43`, `skipped=1`, `failed=0`; único `skipped` classificado como `missing_required` e mantido sem alteração. Auditoria pós-backfill: `legacy_fallback=0`, `divergent=0`, `invalid_reference=0`, `matching=78`, `missing_required=1`. Guard rails pós-backfill: `finance:audit-member-monthly-fees --fail-on-divergence` `EXIT_CODE=0` e `--fail-on-fallback` `EXIT_CODE=0`. Confirmada preservação física de `users.tipo_mensalidade` (sem cleanup destrutivo e sem migration de remoção nesta sprint). | `app/Services/Financeiro/MemberMonthlyFeeLegacyBackfillService.php`, `app/Console/Commands/Financeiro/BackfillMemberMonthlyFeesCommand.php`, `bootstrap/app.php`, `tests/Feature/Financeiro/MemberMonthlyFeeLegacyBackfillServiceTest.php`, `tests/Feature/Financeiro/BackfillMemberMonthlyFeesCommandTest.php`, `storage/app/audits/member-monthly-fees-preflight.json`, `storage/app/audits/member-monthly-fees-backfill.json`, `storage/app/audits/member-monthly-fees-post-backfill.json`, `php artisan finance:audit-member-monthly-fees --fail-on-divergence`, `php artisan finance:audit-member-monthly-fees --fail-on-fallback` | 89% | 90% | `users.tipo_mensalidade` permanece como compatibilidade temporária por desenho (dual-write ativo). O caso `missing_required` permanece isolado para análise funcional separada, sem inferência automática de plano nesta sprint. |
| 2026-07-07 | Membros / Financeiro | Sprint F2 — consolidação do plano de mensalidade canónico do membro (`dados_financeiros.mensalidade_id`) concluída sem cleanup físico. Entregues `MemberMonthlyFeeResolver` (leitura central com prioridade canónica e fallback legacy auditável), `MemberMonthlyFeeSyncService` (escrita central com validação de referência e dual-write temporário em `users.tipo_mensalidade`), integração em `MembrosController` (show/edit/store/update), integração em `MemberImportService` (write path único), atualização de `MonthlyFeeGenerationService` para resolver plano via serviço central, warning explícito `monthly_fee_legacy_fallback` quando há fallback legacy e revisão de queries para priorizar canónico com fallback comentado `TEMP LEGACY F2`. Entregue comando read-only `finance:audit-member-monthly-fees` com `--json`, `--fail-on-divergence`, `--fail-on-fallback`, `--report-path` e `--user`. Testes F2 executados e verdes (52 testes / 241 asserts). Auditoria no dataset alvo: `total_users=79`, `canonical_only=0`, `matching=35`, `legacy_fallback=43`, `divergent=0`, `missing=1`, `missing_required=1`, `not_required=0`, `invalid_reference=0`; guard rails: `--fail-on-divergence` `EXIT_CODE=0`, `--fail-on-fallback` `EXIT_CODE=1`. Decisão factual: abrir F2.1/F2.2 para plano de backfill/controlos antes de remover fallback legacy; não executar backfill nesta sprint. | `app/Services/Financeiro/MemberMonthlyFeeResolver.php`, `app/Services/Financeiro/MemberMonthlyFeeSyncService.php`, `app/Services/Financeiro/MonthlyFeeGenerationService.php`, `app/Http/Controllers/MembrosController.php`, `app/Services/Members/MemberImportService.php`, `app/Http/Controllers/FinanceiroController.php`, `app/Console/Commands/Financeiro/AuditMemberMonthlyFeesCommand.php`, `bootstrap/app.php`, `tests/Feature/Membros/MemberMonthlyFeeResolverTest.php`, `tests/Feature/Financeiro/MemberMonthlyFeeSyncServiceTest.php`, `tests/Feature/Financeiro/MemberMonthlyFeeAuditCommandTest.php`, `tests/Feature/Financeiro/MonthlyFeeGenerationFlowTest.php`, `tests/Feature/Membros/MembrosCurrentAccountSurfaceTest.php`, `tests/Feature/Membros/MemberImportCanonicalWriteContractTest.php`, `storage/app/audits/member-monthly-fees-audit.json`, `storage/app/audits/member-monthly-fees-audit-json-output.txt`, `storage/app/audits/member-monthly-fees-fail-on-divergence-output.txt`, `storage/app/audits/member-monthly-fees-fail-on-fallback-output.txt` | 86% | 87% | Sem remoção de colunas e sem backfill automático por desenho da F2. Dependência real de fallback legacy (`43` membros) permanece e deve ser tratada em F2.1/F2.2 (backfill canónico + redução progressiva de fallback) antes de qualquer cleanup físico de `users.tipo_mensalidade`. |
| 2026-07-07 | Membros / Financeiro | Sprint F1.2 — backfill conservador dos centros de custo legacy-only concluído com write path canónico (`MemberCostCenterSyncService`) e preflight bloqueante (`divergent=0`, `invalid_legacy=0`, `invalid_weights=0`). Entregues `MemberCostCenterLegacyBackfillService` e comando `finance:backfill-member-cost-centers` com `--apply`, `--json`, `--report-path`, `--fail-on-skipped` e `--user`; execução no dataset alvo: preflight dry-run com `79` membros analisados, `ready_for_backfill=78`, `already_canonical=0`, `no_source=1`, zero bloqueios; apply executado com `migrated=78`, `skipped=1`, `failed=0`. Auditoria pós-backfill confirmou `legacy_fallback=0`, `divergent=0`, `invalid_weight=0`, `missing_required=0`; guard rails `--fail-on-divergence` e `--fail-on-fallback` terminaram com `EXIT_CODE=0`. Coluna legacy `users.centro_custo` mantida intacta (sem remoção física), preservando compatibilidade temporal. | `app/Services/Financeiro/MemberCostCenterLegacyBackfillService.php`, `app/Console/Commands/Financeiro/BackfillMemberCostCentersCommand.php`, `bootstrap/app.php`, `tests/Feature/Financeiro/MemberCostCenterLegacyBackfillCommandTest.php`, `storage/app/audits/member-cost-centers-preflight.json`, `storage/app/audits/member-cost-centers-backfill.json`, `storage/app/audits/member-cost-centers-preflight.json`, `php artisan finance:audit-member-cost-centers --json`, `php artisan finance:audit-member-cost-centers --fail-on-divergence`, `php artisan finance:audit-member-cost-centers --fail-on-fallback` | 85% | 86% | Manter `users.centro_custo` apenas como compatibilidade temporária enquanto o fallback legacy permanece ativado por desenho; cleanup físico de schema continua fora de escopo desta sprint. |
| 2026-07-07 | Membros / Financeiro | F1.1 — auditoria factual dos centros de custo canónicos no dataset alvo (modo read-only). Execução de `finance:audit-member-cost-centers --json` com `total_users_analyzed=79`, `legacy_fallback=78`, `divergent=0`, `invalid_weights=0`, `missing_required=0`; classificação derivada do dataset completo: `canonical_only=0`, `legacy_fallback=78`, `divergent=0`, `invalid_weights=0`, `missing_cost_center=0`, `unclassified=1`. Execução de guard rails: `--fail-on-divergence` terminou com `EXIT_CODE=0`; `--fail-on-fallback` terminou com `EXIT_CODE=1`. Decisão: não avançar para cleanup/remoção de fallback nesta sprint; necessário backfill/correção planeada para reduzir fallback real antes de remover `users.centro_custo`. | `storage/app/audits/member-cost-centers/audit-2026-07-07.json`, `storage/app/audits/member-cost-centers/audit-2026-07-07-all-users-classified.json`, `storage/app/audits/member-cost-centers/legacy-fallback-user-ids-2026-07-07.txt`, `storage/app/audits/member-cost-centers/fail-on-divergence-full-2026-07-07.txt`, `storage/app/audits/member-cost-centers/fail-on-fallback-full-2026-07-07.txt`, `app/Console/Commands/Financeiro/AuditMemberCostCentersCommand.php` | 85% | 85% | Dataset alvo auditado sem escrita de dados. Existe dependência real de fallback legacy (78 membros). Divergências entre canónico e legacy não foram encontradas neste dataset. Próxima sprint deve priorizar plano de backfill/sync canónico antes de qualquer remoção de `users.centro_custo`. |
| 2026-07-03 | Membros / Financeiro | Sprint F1 concluída para consolidar os centros de custo canónicos do membro. Criado `MemberCostCenterResolver` para priorizar `centro_custo_user`/`centrosCusto()` e manter `users.centro_custo` apenas como fallback legacy auditável; `MembrosController@show` e `@edit` passaram a expor `centro_custo` e `centro_custo_pesos` através do resolver; `MembrosController@store` e `@update` centralizaram a sincronização do pivot com dual-write temporário para `users.centro_custo`; `MonthlyFeeGenerationService` passou a distribuir mensalidades pelo resolver e a emitir `Log::warning` quando recorre ao fallback legacy; criado comando `finance:audit-member-cost-centers` com `--json`, `--fail-on-divergence` e `--fail-on-fallback`; testes cobrem resolução canónica, fallback, divergência, geração de mensalidades e contrato JSON/guard rails. | `app/Services/Financeiro/MemberCostCenterResolver.php`, `app/Http/Controllers/MembrosController.php`, `app/Services/Financeiro/MonthlyFeeGenerationService.php`, `app/Console/Commands/Financeiro/AuditMemberCostCentersCommand.php`, `bootstrap/app.php`, `tests/Feature/Membros/MemberCostCenterResolverTest.php`, `tests/Feature/Financeiro/MemberCostCenterAuditFlowTest.php` | 84% | 85% | Validar no ambiente alvo a auditoria de divergências e o uso do fallback legacy em mensalidades; manter `users.centro_custo` apenas como compatibilidade temporária até decisão de remoção. |
| 2026-07-03 | Membros / Desportivo | Sprint AC1 concluída para consolidar o tipo funcional de membro com resolver central `MemberTypeResolver` (prioridade canónica `userTypes()`/`user_user_type`, fallback temporário para `users.tipo_membro` apenas quando vazio). `MembrosController@index` passou a calcular estatísticas e contagens funcionais via resolver; `Membros/Show` passou a receber `memberTypes` canónicos para visibilidade do separador Desportivo com fallback frontend; `PrepareTrainingAthletesAction` passou a eleger atletas por relação canónica com fallback controlado, mantendo filtros de estado ativo e ativo desportivo quando aplicável. Entregue comando de auditoria `members:audit-member-types` com `--json` e `--fail-on-divergence` para identificar lacunas de pivots, divergências e mismatches de atleta entre canónico e legacy. Cobertura de testes AC1 adicionada para stats, fallback, payload de ficha, ação de treino e auditoria de divergência. | `app/Services/Members/MemberTypeResolver.php`, `app/Http/Controllers/MembrosController.php`, `resources/js/Pages/Membros/Show.tsx`, `app/Services/Desportivo/PrepareTrainingAthletesAction.php`, `app/Console/Commands/Members/AuditMemberTypesCommand.php`, `bootstrap/app.php`, `tests/Feature/Membros/MemberTypeResolverIntegrationTest.php` | 87% | 88% | Executar auditoria AC1 no ambiente alvo para mapear membros ainda sem pivots `user_user_type` e planear convergência progressiva sem remover `users.tipo_membro` nesta fase. |
| 2026-07-03 | Membros / Legacy cleanup físico | Sprint M6 executada com remoção física conservadora dos campos `users.estado_civil` e `users.numero_irmaos` classificados como `ready_for_cleanup` na M5, mantendo fora de escopo `data_atestado_medico`, `needs_manual_review`, domínio financeiro e dependências desportivas. Migration destrutiva adicionada com rollback estrutural (recria schema, sem reposição automática de valores). Inventário de campos legacy atualizado para categoria documental `removed_after_m5`. Serviço de backfill legacy-only endurecido para tolerar ausência física das colunas removidas sem falhas de query. Testes de regressão adicionados para schema pós-remoção e auditoria M5 verde após drop. | `database/migrations/2026_07_03_130000_drop_estado_civil_numero_irmaos_from_users_table.php`, `app/Services/Members/UsersLegacyOnlyBackfillService.php`, `tests/Feature/Membros/UsersLegacyPhysicalCleanupSchemaTest.php`, `tests/Feature/Membros/LegacyCleanupReadinessAuditCommandTest.php`, `config/member_user_legacy_fields.php`, `docs/MEMBERS_LEGACY_CLEANUP_M5.md` | 86% | 87% | Executar validação no ambiente alvo com auditorias e smoke tests de Ficha/Portal/Família/Financeiro para confirmar ausência de regressões operacionais antes de expandir remoção para outros campos. |
| 2026-07-03 | Membros / Legacy cleanup readiness | Sprint M5 concluída em modo estritamente read-only para preparar cleanup físico futuro de campos legacy ready_for_cleanup em `users`, sem qualquer remoção de coluna e sem novo backfill. Criado comando `members:audit-legacy-cleanup-readiness` com `--json`, `--fail-on-not-ready` e `--field` (`estado_civil`, `numero_irmaos`, `declaracao_transporte`) para validar por campo: destino canónico, migração relevante concluída, ausência de divergências, ausência de leituras/escritas proibidas, classificação `ready_for_cleanup` e plano de rollback documentado. Entregue documentação M5 dedicada com fecho M4, referência ao commit `4f9fb28`, referência à issue `#64`, listas de ready/bloqueados/manual review, rollback e critérios para futura remoção física. Cobertura de testes adicionada para cenário happy-path de `estado_civil` e `numero_irmaos`, falhas por divergência, falhas por leitura/escrita proibida, contrato JSON e `--fail-on-not-ready`. | `app/Services/Members/LegacyCleanupReadinessAuditor.php`, `app/Console/Commands/Members/AuditLegacyCleanupReadinessCommand.php`, `bootstrap/app.php`, `tests/Feature/Membros/LegacyCleanupReadinessAuditCommandTest.php`, `docs/MEMBERS_LEGACY_CLEANUP_M5.md`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 85% | 85% | Executar auditoria M5 no ambiente alvo para confirmar status final por campo antes de qualquer sprint de migration destrutiva; manter issue `#64` isolada no domínio Desportivo sem migração automática dos 5 casos pendentes. |
| 2026-07-03 | Membros / Desportivo | Tarefa D1 concluída em modo read-only para a pendência de `users.data_atestado_medico` sem alvo seguro em `athlete_sports_data`: criado comando `members:audit-pending-medical-certificate-backfill` para auditar exclusivamente os 5 `user_id` pendentes com classificação por caso (`ready_for_backfill`, `invalid_legacy_date`, `not_sports_member`, `missing_sports_target`, `already_matching`, `divergent`), validação de data legacy com bloqueio de anos anómalos (ex.: `0024`/`0023`), relatório humano/JSON e escrita opcional em `--report-path`, sem qualquer escrita em BD. Cobertura de testes adicionada para escopo fechado dos 5 IDs, classificações pedidas, garantia read-only e outputs JSON/report. | `app/Services/Members/PendingMedicalCertificateBackfillAuditor.php`, `app/Console/Commands/Members/AuditPendingMedicalCertificateBackfillCommand.php`, `bootstrap/app.php`, `tests/Feature/Membros/PendingMedicalCertificateBackfillAuditCommandTest.php` | 85% | 85% | Executar o comando no ambiente alvo para decidir os casos `missing_sports_target`/`ready_for_backfill` antes de qualquer comando de backfill; tratar manualmente datas legacy inválidas classificadas como `invalid_legacy_date`. |
| 2026-07-03 | Membros / Legacy-only final backfill | Correção definitiva da M4.17 com schema real: adição aditiva de `dados_pessoais.estado_civil` e `dados_pessoais.numero_irmaos`, backfill de `estado_civil`/`numero_irmaos` para `dados_pessoais` e de `data_atestado_medico` para `athlete_sports_data`, sem tocar em `users.*`. O preflight passou a considerar resolvível o destino físico quando a tabela/colunas existem e o commit gate passou a distinguir bloqueadores reais de execuções idempotentes sem candidatos. Cobertura de testes reforçada para criação mínima de row em `dados_pessoais`, criação segura de `athlete_sports_data` apenas para atleta ativo, skips de não-atletas e ausência de overwrite em divergências. | `database/migrations/2026_07_03_120000_add_estado_civil_numero_irmaos_to_dados_pessoais_table.php`, `app/Services/Members/UsersLegacyOnlyBackfillService.php`, `app/Services/Members/UsersLegacyOnlyBackfillPreflightService.php`, `app/Console/Commands/Members/BackfillUsersLegacyOnlyFieldsCommand.php`, `tests/Feature/Membros/UsersLegacyOnlyBackfillCommandTest.php`, `tests/Feature/Membros/UsersLegacyOnlyBackfillPreflightCommandTest.php` | 86% | 86% | Validar no ambiente alvo os relatórios dry-run/commit e confirmar o comportamento idempotente nos campos já preenchidos. |
| 2026-07-01 | Membros / Identidade display | Sprint M4.9-F6 — criado `MemberIdentityDisplayResolver` para nome de apresentação canónico (fonte principal `dados_pessoais.nome_completo`, fallback controlado `users.nome_completo` e `users.name`) e migração das leituras de display em controllers/model/service alvo (`Configuracoes`, `Dashboard`, `Equipas`, `Eventos`, `FamilyPortal`, `Logistica`, `Membros`, `PortalEvent`, `PortalPage`, `PortalProfile`, `PortalTraining`, `ConvocationGroup`, `FamilyService` e `MemberDataMigrationService`). Scanner atualizado para allowlist apenas do resolver transicional autorizado. Resultado local da auditoria: `member_identity_display` reduziu de `26` para `0`; findings totais de `41` para `15` (restantes apenas `sports_member_payload`, `communication_segments` e `legacy_command_or_backfill`). Sem alterações de BD/migrations/colunas/casts/fillable. | `app/Services/Members/MemberIdentityDisplayResolver.php`, `app/Services/Members/UsersLegacyReadScanner.php`, `app/Http/Controllers/ConfiguracoesController.php`, `app/Http/Controllers/DashboardController.php`, `app/Http/Controllers/EquipasController.php`, `app/Http/Controllers/EventosController.php`, `app/Http/Controllers/FamilyPortalController.php`, `app/Http/Controllers/LogisticaController.php`, `app/Http/Controllers/MembrosController.php`, `app/Http/Controllers/PortalEventController.php`, `app/Http/Controllers/PortalPageController.php`, `app/Http/Controllers/PortalProfileController.php`, `app/Http/Controllers/PortalTrainingController.php`, `app/Models/ConvocationGroup.php`, `app/Services/Family/FamilyService.php`, `app/Services/Members/MemberDataMigrationService.php`, `tests/Feature/Membros/MemberIdentityDisplayResolverTest.php`, `tests/Feature/Membros/MemberIdentityDisplayCanonicalReadTest.php`, `tests/Feature/Membros/UsersLegacyReadAuditCommandTest.php`, `storage/app/audits/m4-9-f6-users-legacy-read-after-identity-display.json` | 85% | 85% | Validar no ambiente alvo com dados reais e manter fora de escopo nesta sprint os grupos `sports_member_payload`, `communication_segments` e `legacy_command_or_backfill`. |
| 2026-07-03 | Membros / Contrato canónico dados pessoais | Sprint M4.16 — `estado_civil` formalizado como chave canónica reconhecida no contrato de `dados_pessoais` (camada de leitura canónica), mantendo fase estritamente read-only: sem backfill, sem escrita BD, sem migrations, sem alterações em `users.*`, sem alterações de casts e sem alterações de `User::$fillable`. Config de canonical targets evoluída para `target_status=canonical_payload_key_defined` com `write_allowed=false`; preflight M4.14 passou a classificar `estado_civil` como destino definido mas bloqueado para escrita e o auditor M4.15 passou a aceitar/reportar este status. Auditoria M4.13 ajustada para considerar definição contratual explícita e classificar `estado_civil` como `needs_backfill` quando aplicável, sem qualquer escrita. | `config/member_user_legacy_canonical_targets.php`, `app/Services/Members/MemberDataReadService.php`, `app/Services/Members/UsersLegacyOnlyBackfillPreflightService.php`, `app/Services/Members/UsersLegacyCanonicalTargetDecisionAuditor.php`, `app/Console/Commands/Members/PreflightUsersLegacyOnlyBackfillCommand.php`, `app/Console/Commands/Members/AuditUsersLegacyCanonicalTargetsCommand.php`, `app/Services/Members/UsersLegacyBackfillValidationAuditor.php`, `tests/Feature/Membros/MemberPersonalCanonicalContractTest.php`, `tests/Feature/Membros/UsersLegacyCanonicalTargetDecisionAuditCommandTest.php`, `tests/Feature/Membros/UsersLegacyOnlyBackfillPreflightCommandTest.php`, `tests/Feature/Membros/UsersLegacyBackfillValidationAuditCommandTest.php` | 85% | 85% | Próximo passo: sprint dedicada para validar backfill controlado apenas de `estado_civil` com critérios de segurança e rollback, mantendo `write_allowed=false` até validação operacional no ambiente alvo. |
| 2026-07-02 | Membros / Legacy-only canonical targets | Sprint M4.15 — criada matriz configurável de decisões canónicas para os três campos legacy_only bloqueados no preflight M4.14 (`data_atestado_medico`, `estado_civil`, `numero_irmaos`) com `write_allowed=false` para todos, sem backfill e sem escrita em BD. Entregue auditoria read-only `members:audit-users-legacy-canonical-targets` (humano/JSON, filtro por campo, fail flags e report path) e enriquecimento do preflight para expor `target_status`, `decision`, `owner_area`, `reason` e `next_action`, mantendo `writable=false` e `commit_allowed=false` mesmo com `--commit`. | `config/member_user_legacy_canonical_targets.php`, `app/Services/Members/UsersLegacyCanonicalTargetDecisionAuditor.php`, `app/Console/Commands/Members/AuditUsersLegacyCanonicalTargetsCommand.php`, `app/Services/Members/UsersLegacyOnlyBackfillPreflightService.php`, `tests/Feature/Membros/UsersLegacyCanonicalTargetDecisionAuditCommandTest.php`, `tests/Feature/Membros/UsersLegacyOnlyBackfillPreflightCommandTest.php`, `bootstrap/app.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 85% | 85% | Próxima sprint deve decidir o contrato canónico para `dados_pessoais` (`estado_civil`, `numero_irmaos`) e a correspondência segura com `athlete_sports_data` para `data_atestado_medico` antes de qualquer backfill. |
| 2026-07-02 | Membros / Legacy-only backfill preflight | Sprint M4.14 reformulada como preflight read-only para os três campos legacy_only em `users` (`data_atestado_medico`, `estado_civil`, `numero_irmaos`). Criado `members:preflight-users-legacy-only-backfill` para diagnosticar contagem legacy_only, estado do destino canónico e bloqueio explícito de `--commit` enquanto os destinos aprovados não existirem no schema versionado. O caso `data_atestado_medico` aponta o destino proposto para `athlete_sports_data` e os restantes dois campos continuam sem destino canónico versionado em `dados_pessoais`. Sem escrita em BD, sem migrations, sem alterações a `users.*`, sem casts e sem `User::$fillable`. | `app/Services/Members/UsersLegacyOnlyBackfillPreflightService.php`, `app/Console/Commands/Members/PreflightUsersLegacyOnlyBackfillCommand.php`, `tests/Feature/Membros/UsersLegacyOnlyBackfillPreflightCommandTest.php`, `bootstrap/app.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 85% | 85% | Decisão arquitetural necessária antes de qualquer backfill: `data_atestado_medico` deve ou não convergir para `athlete_sports_data`; `estado_civil` e `numero_irmaos` carecem de schema canónico versionado. |
| 2026-07-02 | Membros / Readiness legacy users | Sprint M4.12 — criada matriz explícita de decisões read-only para os 23 campos ainda em `needs_review` na auditoria M4.11, através de `config/member_user_legacy_removal_decisions.php` e evolução do auditor/comando `members:audit-users-legacy-field-removal-readiness`. A classificação passou a separar `keep_operational_explicit`, `candidate_after_legacy_write_cleanup`, `candidate_after_backfill_validation`, `needs_business_decision`, `keep_historical_or_external_reference` e `keep_until_module_refactor`, mantendo compatibilidade com `removal_status`, `risk`, `canonical_area`, `canonical_field`, `passed` e `failure_reason`. Resultado local: `unknown_count`/`unclassified_count` desceram de `23` para `0`, `active_legacy_read_fields_count` manteve `0`, `explicit_decisions_count=23`, `inferred_decisions_count=44`; `telefone`, `numero_cartao_cidadao` e `validade_cartao_cidadao` deixaram de ficar sem decisão e passaram para `needs_business_decision`. Comandos validados localmente: `php artisan members:audit-users-legacy-field-removal-readiness`, `php artisan members:audit-users-legacy-field-removal-readiness --json`, `php artisan members:audit-users-legacy-field-removal-readiness --show-decisions`, `php artisan members:audit-users-legacy-read --fail-on-finding`, `php artisan members:audit-users-legacy-field-map --json --fail-on-unknown` e `php artisan members:audit-users-legacy-write-guard --json --fail-on-violation`. Escopo mantido documental/audit-only: sem remoção de colunas, sem migrations, sem alteração de dados, sem alteração de lógica funcional, sem alteração de `User::$fillable` e sem alteração de casts. | `config/member_user_legacy_removal_decisions.php`, `app/Services/Members/UsersLegacyFieldRemovalReadinessAuditor.php`, `app/Console/Commands/Members/AuditUsersLegacyFieldRemovalReadinessCommand.php`, `tests/Feature/Membros/UsersLegacyFieldRemovalReadinessAuditCommandTest.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md`, `storage/app/audits/m4-12-users-legacy-field-removal-readiness-decisions.json` | 85% | 85% | Próxima fase pode escolher um subgrupo `candidate_after_legacy_write_cleanup` para isolamento controlado apenas após validação adicional de backfill/cobertura; esta sprint não remove colunas nem altera runtime funcional. |
| 2026-07-02 | Membros / Inventário legacy users | Sprint M4.11 — criado inventário read-only de readiness para futura remoção/isolamento dos campos legacy de membro em `users`, cruzando schema real, mapa técnico de campos (`member_user_legacy_fields`), bloqueio de escrita legacy, auditoria de leitura legacy e equivalências canónicas em `MemberDataReadService` (`PERSONAL_FALLBACK_MAP` / `CONFIGURATION_FALLBACK_MAP`). Entregue comando `members:audit-users-legacy-field-removal-readiness` com saída humana/JSON e matriz por `category`, `removal_status` e `risk`; mantido escopo sem remoção de colunas, sem migrations, sem alteração de lógica funcional, sem alteração de `User::$fillable` e sem alteração de casts. Guard rails M4.10 mantidos e esta auditoria permanece relatório de apoio (não bloqueante de CI nesta fase). | `app/Services/Members/UsersLegacyFieldRemovalReadinessAuditor.php`, `app/Console/Commands/Members/AuditUsersLegacyFieldRemovalReadinessCommand.php`, `tests/Feature/Membros/UsersLegacyFieldRemovalReadinessAuditCommandTest.php`, `bootstrap/app.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 85% | 85% | Validar execução no ambiente alvo e usar matriz M4.11 para planear fase futura de isolamento/remoção sem regressões. |
| 2026-07-02 | Membros / Auditoria legacy users | Sprint M4.10 — `members:audit-users-legacy-read` promovido para guard rail bloqueante obrigatório em validação (`--fail-on-finding`), após fecho da M4.9 com `findings_count=0`. Qualquer regressão com nova leitura direta de campos legacy de membro em `users` deve falhar a validação. Este guard rail junta-se aos obrigatórios `members:audit-data-fallback --fail-on-fallback`, `members:audit-users-legacy-field-map --json --fail-on-unknown` e `members:audit-users-legacy-write-guard --json --fail-on-violation`. | `docs/ESTADO_VIVO_DESENVOLVIMENTO.md`, `.github/workflows/ci.yml`, `tests/Feature/Membros/UsersLegacyReadGuardRailTest.php`, `tests/Feature/Membros/UsersLegacyReadAuditCommandTest.php` | 85% | 85% | Sem alterações funcionais de runtime, sem alterações de BD/migrations/colunas/casts/`User::$fillable`; manter guard rails obrigatórios em validações locais e pipeline. |
| 2026-07-02 | Membros / Auditoria legacy users | Sprint M4.9-F9 — fechado o último finding do grupo `legacy_command_or_backfill` em `BackfillFinanceiroIntegracoes`: leitura direta de `users.nome_completo` substituída por `MemberIdentityDisplayResolver` no contexto de descrição de itens de convocatória, preservando comportamento do comando/backfill e sem allowlist ampla. Auditoria `members:audit-users-legacy-read` passou localmente para `findings_count=0` e `by_remediation_group` vazio. Guard rails de mapa de campos e write guard mantidos verdes. | `app/Console/Commands/BackfillFinanceiroIntegracoes.php`, `tests/Feature/Membros/LegacyCommandBackfillCanonicalReadTest.php`, `tests/Feature/Membros/UsersLegacyReadAuditCommandTest.php`, `storage/app/audits/m4-9-f9-users-legacy-read-final-zero.json` | 85% | 85% | Sem alterações de BD/migrations/colunas/casts/`User::$fillable`; validar no ambiente alvo com dataset real e manter auditorias M4.9 no pipeline de release. |
| 2026-07-02 | Membros / Comunicação | Sprint M4.9-F8 — leituras legacy `users` em comunicação/segmentação migradas para resolvers canónicos: `ComunicacaoController` passou a montar autores/destinatários com `MemberIdentityDisplayResolver` e `MemberDataReadService`, `InternalCommunicationService` passou a resolver nomes de envio/caixa interna por `MemberIdentityDisplayResolver` e `SegmentResolverService` passou a resolver nome/contacto canónicos para destinatários de segmentos. `communication_segments` ficou fechado localmente sem allowlist nova e sem alterações de BD, migrations, colunas, casts ou `User::$fillable`. | `app/Http/Controllers/ComunicacaoController.php`, `app/Services/Communication/InternalCommunicationService.php`, `app/Services/Communication/SegmentResolverService.php`, `tests/Feature/Membros/CommunicationSegmentsCanonicalReadTest.php`, `storage/app/audits/m4-9-f8-before.json`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 85% | 85% | Confirmar auditoria global pós-alteração e manter `field-map` / `write-guard` verdes no ambiente alvo. |
| 2026-07-02 | Membros / Desportivo | Sprint M4.9-F7 — leituras legacy `users` no payload desportivo migradas para resolvers canónicos: `AthleteController` passou a resolver `nome_completo` por `MemberIdentityDisplayResolver` e `data_nascimento` por `MemberDataReadService`; `DesportivoPagePayloadBuilder` passou a resolver `nome_completo`, `num_federacao`, `data_atestado_medico` e `informacoes_medicas` via resolvers (`MemberIdentityDisplayResolver` + `MemberDocumentDataResolver::sportsPayload`). Mantido shape de payload/API e sem allowlist nova em controllers/services desportivos. | `app/Http/Controllers/Api/AthleteController.php`, `app/Services/Desportivo/DesportivoPagePayloadBuilder.php`, `app/Services/Members/MemberDocumentDataResolver.php`, `tests/Feature/Membros/SportsMemberPayloadCanonicalReadTest.php`, `tests/Feature/Membros/MemberDocumentDataResolverTest.php`, `storage/app/audits/m4-9-f7-users-legacy-read-after-sports-payload.json` | 85% | 85% | Validar auditoria pós-alteração com redução/fecho de `sports_member_payload` e manter guard rails (`field-map` e `write-guard`) verdes no ambiente alvo. |
| 2026-07-01 | Membros / Financeiro Fiscal | Sprint M4.9-F5 — leituras fiscais/financeiras legacy de `users` em contexto financeiro migradas para `MemberFiscalDataResolver` em `FinanceiroController`, `BankReconciliationSuggestionService`, `FinanceReportService` e `ReceiptMatchingService`, com ajuste de heurística no scanner para não classificar contexto de fornecedor (`supplier`) como leitura de membro. Mantido comportamento funcional/algoritmos de scoring e matching, sem alterações de BD/migrations/colunas/casts/fillable/frontend. | `app/Http/Controllers/FinanceiroController.php`, `app/Services/Financeiro/BankReconciliationSuggestionService.php`, `app/Services/Financeiro/FinanceReportService.php`, `app/Services/Financeiro/ReceiptMatchingService.php`, `app/Services/Members/MemberFiscalDataResolver.php`, `app/Services/Members/UsersLegacyReadScanner.php`, `tests/Feature/Membros/FinanceiroFiscalCanonicalReadTest.php`, `tests/Feature/Membros/MemberFiscalDataResolverTest.php`, `tests/Feature/Membros/UsersLegacyReadAuditCommandTest.php`, `storage/app/audits/m4-9-f5-users-legacy-read-after-fiscal-finance.json` | 85% | 85% | Confirmar redução do grupo `member_fiscal_finance` para 0 e validação local/ambiente alvo dos comandos de auditoria/guard rails sem allowlist em controllers/services financeiros. |
| 2026-07-01 | Membros / Auditoria legacy users | Sprint M4.9-F4B — fechados os findings residuais do grupo `member_personal_profile` através de migração real de leituras pessoais em `MembrosController` e `MemberFiscalDataResolver` para `MemberDataReadService` (`personalPayload`/`valueFromPersonal`), mantendo shape funcional e fallback seguro. Cobertura adicionada para scanner por path e leitura canónica/fallback sem allowlist nova em ficheiros funcionais. | `app/Http/Controllers/MembrosController.php`, `app/Services/Members/MemberFiscalDataResolver.php`, `tests/Feature/Membros/MembrosControllerPersonalCanonicalReadTest.php`, `tests/Feature/Membros/MemberFiscalDataResolverTest.php`, `storage/app/audits/m4-9-f4b-users-legacy-read-after-personal-residual.json` | 85% | 85% | Sem alterações de BD/migrations/colunas/casts/frontend; grupos `member_identity_display`, `member_fiscal_finance`, `sports_member_payload` e `communication_segments` continuam fora do escopo desta sprint. |
| 2026-06-26 | Membros / Portal Profile | Sprint M4.9-F4A — `PortalProfileController` passou a ler o bloco pessoal/editável do perfil via `MemberDataReadService::personalPayload()`, preservando shape/props/rotas/permissões e mantendo a escrita do portal via `MemberDataWriteService`. A leitura documental continua em `MemberDocumentDataResolver`; não houve alterações de BD, migrations, colunas, casts ou frontend. | `app/Http/Controllers/PortalProfileController.php`, `tests/Feature/Membros/PortalProfilePersonalCanonicalReadTest.php`, `tests/Feature/Membros/UsersLegacyReadAuditCommandTest.php` | 85% | 85% | Redução esperada do grupo `member_personal_profile` e dos findings totais da auditoria; sem mudanças em Financeiro/Desportivo/Comunicação e sem cutover adicional de `member_identity_display`. |
| 2026-06-26 | Membros / Documentos portal | Sprint M4.9-F3C — `PortalProfileController` passou a resolver leituras documentais/configuração (RGPD, consentimentos, atestado, afiliação/federação e número federativo em contexto documental) via `MemberDocumentDataResolver::profileDocuments()`, preservando shape/labels/textos do payload do perfil e sem alterar blocos pessoais. Scanner mantém `PortalProfileController` fora da allowlist e o objetivo é fechar o grupo `member_documents_configuration` para este controller. | `app/Http/Controllers/PortalProfileController.php`, `app/Services/Members/MemberDocumentDataResolver.php`, `tests/Feature/Membros/MemberDocumentDataResolverTest.php`, `tests/Feature/Membros/PortalProfileDocumentResolverReadTest.php`, `tests/Feature/Membros/UsersLegacyReadAuditCommandTest.php` | 85% | 85% | Sem alterações de BD/migrations/colunas/casts/frontend; sem mudanças em Financeiro/Desportivo; bloco pessoal do Portal Profile mantém migração para sprint M4.9-F4. |
| 2026-06-26 | Membros / Documentos admin | Sprint M4.9-F3B — `MembrosController` passou a resolver paths e estado documental via `MemberDocumentDataResolver` na ficha admin (show/edit/update/destroy), mantendo shape do payload e deletes compatíveis; scanner deixou de reportar findings documentais no controller sem allowlist nova. | `app/Http/Controllers/MembrosController.php`, `app/Services/Members/MemberDocumentDataResolver.php`, `app/Services/Members/UsersLegacyReadScanner.php`, `tests/Feature/Membros/MemberDocumentDataResolverTest.php`, `tests/Feature/Membros/MembrosControllerDocumentResolverReadTest.php`, `tests/Feature/Membros/UsersLegacyReadAuditCommandTest.php` | 85% | 85% | Redução esperada do grupo `member_documents_configuration` abaixo dos 23 findings anteriores; sem alterações de BD/migrations/colunas/casts/frontend. |
| 2026-06-26 | Membros / Documentos portal | Sprint M4.9-F3A — criado `MemberDocumentDataResolver` como camada canónica/transicional para documentos/configuração com fonte principal em `MemberDataReadService::configurationPayload($user)` e fallback legacy documental concentrado no resolver; `PortalDocumentController` migrado para usar o resolver (sem leituras diretas legacy documentais em `users` no controller), scanner atualizado para allowlist explícita do resolver transicional e cobertura adicionada para resolver/controller/auditoria. | `app/Services/Members/MemberDocumentDataResolver.php`, `app/Http/Controllers/PortalDocumentController.php`, `app/Services/Members/UsersLegacyReadScanner.php`, `tests/Feature/Membros/MemberDocumentDataResolverTest.php`, `tests/Feature/Membros/PortalDocumentControllerCanonicalReadTest.php`, `tests/Feature/Membros/UsersLegacyReadAuditCommandTest.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 85% | 85% | Sem alterações de BD/migrations/colunas/casts/frontend; `MembrosController` e `PortalProfileController` fora do escopo desta sprint. |
| 2026-06-26 | Membros / Auditoria legacy users | Sprint M4.9-F2 — camada de triagem/classificação dos findings de leitura legacy em `users` adicionada ao scanner/comando de auditoria, com metadados por finding (`severity`, `remediation_group`, `remediation_hint`, `migration_priority`), resumo agrupado (`grouped_summary`) e opções de saída `--group-by`/`--summary-only`. Objetivo: preparar M4.9-F3 por grupos sem corrigir ainda controllers/services. | `app/Services/Members/UsersLegacyReadScanner.php`, `app/Console/Commands/Members/AuditUsersLegacyReadCommand.php`, `tests/Feature/Membros/UsersLegacyReadAuditCommandTest.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 85% | 85% | Sem correção funcional de leituras legacy, sem alteração de BD/migrations, sem alteração de `MemberDataReadService`, `MemberFiscalDataResolver`, frontend, casts de `User` ou fluxos de escrita. |
| 2026-06-26 | Membros / Auditoria legacy users | Sprint M4.9-F1 — criada auditoria read-only para inventariar leituras diretas de campos pessoais/configuração legacy em `users` fora dos serviços/comandos autorizados, com scanner estático (`UsersLegacyReadScanner`), comando `members:audit-users-legacy-read` (`--json`, `--fail-on-finding`, `--report-path`, `--path`) e suíte de testes dedicada. Objetivo: mapear dependências residuais antes de remover casts/fallback, sem alterações funcionais. | `app/Services/Members/UsersLegacyReadScanner.php`, `app/Console/Commands/Members/AuditUsersLegacyReadCommand.php`, `tests/Feature/Membros/UsersLegacyReadAuditCommandTest.php`, `bootstrap/app.php` | 85% | 85% | Sem alterações de BD/migrations, sem alteração de casts, sem alteração de `MemberDataReadService`; findings reais (se existirem) ficam apenas reportados nesta fase. |
| 2026-06-25 | Membros / Guard rails legacy users | Sprint M4.8-F1 — `User::$fillable` endurecido contra campos personal/config legacy e removido `data_validade_cc` do mass assignment por não pertencer ao schema real; casts e colunas legacy foram mantidos nesta fase (sem cutover). | `app/Models/User.php`, `tests/Feature/Membros/UserLegacyMassAssignmentGuardTest.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 85% | 85% | Guard rail de mass assignment reforçado; próximos passos de descontinuação de colunas legacy continuam dependentes das auditorias de fallback e mapa de campos. |
| 2026-06-25 | Membros / Guard rails legacy users | Sprint M4.7-F1 — criado guard rail automatizado para prevenir regressões de escrita direta de campos pessoais/configuração legacy em `users`, com scanner estático dedicado, comando read-only `members:audit-users-legacy-write-guard` (`--json`, `--fail-on-violation`, `--report-path`) e suite de testes cobrindo baseline sem violações, deteção de exemplos artificiais e allowlist de leitura/auditoria/backfill protegido. Sem alterações funcionais de runtime, sem remoção de colunas e sem migrations. | `app/Services/Members/UsersLegacyWriteGuardScanner.php`, `app/Console/Commands/Members/AuditUsersLegacyWriteGuardCommand.php`, `tests/Feature/Membros/UsersLegacyWriteGuardTest.php`, `bootstrap/app.php` | 85% | 85% | Guard rail estático foca padrões óbvios de escrita e depende de allowlist explícita para contextos históricos/read-only. |
| 2026-06-25 | Membros / Inventário legacy users | Sprint M4.6-F2 — mapa legacy `users` alinhado com o schema real: removida a referência documental a `data_validade_cc` do inventário e do mapeamento `field_to_category`. A auditoria `members:audit-users-legacy-field-map` deve manter `unknown_columns=0`, passar a `missing_configured_columns=0` e continuar com `classified_columns` igual a `total_users_columns`. | `config/member_user_legacy_fields.php`, `tests/Feature/Membros/UsersLegacyFieldMapAuditCommandTest.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 85% | 85% | Sem alterações de BD, sem migrations, sem alterações de runtime funcional (apenas alinhamento documental e de teste). |
| 2026-06-25 | Membros / Inventário legacy users | Sprint M4.6-F1 — criado inventário técnico versionado dos campos legacy de `users` em `config/member_user_legacy_fields.php` com categorias operacionais/pessoais/configuração/financeiro/desportivo/família/revisão, mapa `field_to_category` e pré-requisitos de remoção. Criado comando read-only `members:audit-users-legacy-field-map` com saída JSON, guarda `--fail-on-unknown` e relatório de `missing_configured_columns` para auditar o schema atual sem escrita. Não houve remoção de campos, migrations, controllers ou frontend. | `config/member_user_legacy_fields.php`, `app/Console/Commands/Members/AuditUsersLegacyFieldMapCommand.php`, `tests/Feature/Membros/UsersLegacyFieldMapAuditCommandTest.php`, `bootstrap/app.php` | 85% | 85% | Inventário documental; `data_validade_cc` aparece como campo configurado em falta no schema atual e deve permanecer em revisão até cutover/normalização futura. |
| 2026-06-25 | Membros / Observabilidade | Sprint M4.5-F1 — comando `members:audit-data-fallback` reforçado com guard rail opcional `--fail-on-fallback`, mantendo execução read-only e fallback legado ativo. Em modo guard rail, o comando passa a devolver exit code `1` apenas quando existe fallback real (`personal_fallback_values > 0` ou `configuration_fallback_values > 0`) e mantém `0` quando não há dependência residual; campos vazios continuam não bloqueantes. JSON passou a incluir `passed` e `failure_reason` para uso em CI/local/produção. | `app/Console/Commands/Members/AuditMemberDataFallbackCommand.php`, `tests/Feature/Membros/MemberDataFallbackAuditCommandTest.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 85% | 85% | Não remove fallback, não altera `MemberDataReadService` e funciona como bloqueio operacional para futuras mudanças enquanto existir dependência legacy em `users`. |
| 2026-06-25 | Membros / Dados pessoais | Sprint M4.4-F2 — criado comando seguro `members:backfill-contact` para preencher apenas `dados_pessoais.contacto` quando canónico vazio, usando fallback ordenado de `users` (`contacto` → `telemovel` → `contacto_telefonico`), dry-run por defeito e escrita bloqueada sem confirmação explícita (`--commit --confirm=BACKFILL_CONTACT`). Incluída máscara de preview de contacto, export JSON opcional por `--report-path`, filtros por `--user-id`/`--limit` e suite de testes cobrindo bloqueios de segurança, ordem de fallback, não sobrescrita de canónico e não criação de `dados_pessoais`. | `app/Console/Commands/Members/BackfillMemberContactCommand.php`, `bootstrap/app.php`, `tests/Feature/Membros/MemberContactBackfillCommandTest.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 85% | 86% | Mantido fallback de leitura em `MemberDataReadService`; sem migrations, sem alterações de frontend e sem escrita em `users`/`dados_configuracao`. Validar execução em ambiente alvo antes de commit real em produção. |
| 2026-06-24 | Membros / Observabilidade | Sprint M4.4-F1 — criado comando read-only `members:audit-data-fallback` para auditar dependências de fallback legacy de `users` por utilizador e por campo, com métricas `canonical/fallback/empty`, filtros (`--user-id`, `--limit`, `--only`), inclusão opcional de vazios (`--with-empty-canonical`) e export JSON por `--report-path` sem qualquer escrita em `users`, `dados_pessoais` ou `dados_configuracao`. Incluída suite de testes de feature cobrindo read-only, prioridade canónica, boolean `false`, filtros e escrita de relatório. | `app/Console/Commands/Members/AuditMemberDataFallbackCommand.php`, `bootstrap/app.php`, `tests/Feature/Membros/MemberDataFallbackAuditCommandTest.php` | 85% | 85% | Sem alterações em controllers/frontend/migrations e sem remoção de fallback legacy; próximo passo é usar o relatório para priorizar fecho de dependências residuais em `users` antes de desativação de fallback. |
| 2026-06-24 | Membros / API Users | Sprint M4.3-F2 — `Api\UsersController` refatorado para escrita canónica: `users` passou a receber apenas payload auth/operacional mínimo, enquanto `dados_pessoais`/`dados_configuracao` são persistidos via `MemberDataWriteService` em `store`/`update` com transação. Mantida autenticação, validação existente e compatibilidade de resposta JSON com relações carregadas. | `app/Http/Controllers/Api/UsersController.php`, `tests/Feature/Api/UsersCanonicalWriteContractTest.php`, `tests/Feature/Membros/MemberDataLegacyUserWriteProtectionTest.php`, `tests/Feature/Membros/MemberDataWriteCutoverTest.php` | 84% | 85% | Sem alterações em rotas API, migrations ou frontend. Fallback legado permanece ativo por compatibilidade enquanto decorre o fecho das dependências legacy em `users`. |
| 2026-06-24 | Membros / Importação | Sprint M4.2-F2 — `MemberImportService` deixou de escrever payload pessoal/configuração diretamente em `users` e passou a criar membro com payload auth/operacional mínimo, persistindo `dados_pessoais`/`dados_configuracao` via `MemberDataWriteService` após criação. Mantida a lógica de `dados_financeiros` por `tipo_mensalidade`, sincronização de centros de custo e warning de compatibilidade para `conta_corrente_manual`. | `app/Services/Members/MemberImportService.php`, `tests/Feature/Membros/MemberImportCanonicalWriteContractTest.php` | 56% | 60% | Validado por testes automatizados; sem alterações em controllers/migrations/frontend e sem remoção de fallback/colunas legacy. |
| 2026-06-24 | Financeiro / Emissão fiscal | Sprint M4.1-F3 — `FiscalDocumentRequestService` passou a usar `MemberFiscalDataResolver` na criação de pedidos fiscais a partir de faturas, priorizando `dados_pessoais` para nome/NIF/morada e mantendo fallback para campos legacy em `users`. Mantida validação de dados obrigatórios (`Nome`, `NIF`, `Morada`) sem alterar regras fiscais nem fluxo público de criação. Adicionados testes específicos para prioridade canónica, fallback legacy e erro por NIF em falta via resolver. | `app/Services/Financeiro/FiscalDocumentRequestService.php`, `tests/Feature/Financeiro/FiscalDocumentRequestFlowTest.php`, `app/Services/Members/MemberFiscalDataResolver.php` | 46% | 47% | Validar em ambiente alvo com cenário real de sócios com e sem `dados_pessoais`; restante fluxo Wintouch/manual e anulação fiscal mantém pendências da F6. |
| 2026-06-19 | Membros / Família | Sprint M2.6 — validação operacional do dual-write e preparação de rollout. Adicionado teste dedicado para validação de escrita dual-write (ficha + portal), sincronização legacy em `users`, fallback pós-escrita, idempotência e não regressão em superfícies fora do escopo (Financeiro/Desportivo/Família). Criado runbook de rollout/rollback com comandos permitidos/proibidos, checklist e critérios de sucesso/abortar. | `tests/Feature/Membros/MemberDataDualWriteOperationalValidationTest.php`, `docs/deploy/MEMBERS_M2_6_DUAL_WRITE_ROLLOUT_RUNBOOK.md`, `docs/PLANO_CONSOLIDACAO_MEMBROS.md`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 84% | 84% | Sem nova lógica funcional, sem alterações em UI/migrations/imports/Financeiro/Desportivo/relações familiares, sem deploy e sem execução em produção. Próxima sprint recomendada: M2.7 (rollout controlado no ambiente alvo ou preparação para reduzir dependências legacy em `users`). |
| 2026-06-19 | Membros / Família | Sprint M2.5 — cutover controlado de escrita com dual-write temporário. Criado `MemberDataWriteService` para escrita principal em `dados_pessoais`/`dados_configuracao` e sincronização legacy em `users`; `MembrosController.store/update` e `PortalProfileController.update` passaram a usar o serviço. Inclui criação automática de linhas em falta, atualização seletiva por payload (sem limpar campos ausentes), persistência de booleanos `false`, manutenção de `perfil/email/estado/numero_socio/password` em `users` e atualização de `migration_source_hash`/`migrated_from_users_at` de forma compatível. | `app/Services/Members/MemberDataWriteService.php`, `app/Http/Controllers/MembrosController.php`, `app/Http/Controllers/PortalProfileController.php`, `tests/Feature/Membros/MemberDataWriteCutoverTest.php`, `docs/PLANO_CONSOLIDACAO_MEMBROS.md`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 83% | 84% | Leitura mantém `MemberDataReadService` com fallback; sem alterações em migrations/frontend/imports/Financeiro/Desportivo/relações familiares; sem deploy e sem execução em produção. Próxima sprint recomendada: M2.6 (validação operacional ampliada e preparação da redução gradual de dependências legacy em `users`). |
| 2026-06-19 | Infra / Segurança Operacional | Sprint SEC-1 — guardrails contra destruição acidental da base de dados. Introduzida configuração dedicada (`database_safety`) com bloqueio de `migrate:fresh`, `migrate:refresh`, `migrate:reset` e `db:wipe`; bloqueio absoluto em `production`, por proteção explícita e por assinatura remota (`neon.tech`); bypass permitido apenas em `local/testing` com `DB_ALLOW_DESTRUCTIVE_COMMANDS=true` e `--confirm=DESTROY_LOCAL_DATABASE`. Criado comando alternativo `dev:reset-database --confirm=RESET_LOCAL_DEV` com recusa explícita para Neon. Adicionados testes e runbook de operação/backup. | `config/database_safety.php`, `app/Support/DatabaseSafetyGuard.php`, `app/Console/Commands/DatabaseSafety/MigrateFreshGuardedCommand.php`, `app/Console/Commands/DatabaseSafety/MigrateRefreshGuardedCommand.php`, `app/Console/Commands/DatabaseSafety/MigrateResetGuardedCommand.php`, `app/Console/Commands/DatabaseSafety/DbWipeGuardedCommand.php`, `app/Console/Commands/DevResetDatabaseCommand.php`, `bootstrap/app.php`, `.env.example`, `tests/TestCase.php`, `tests/Feature/Infrastructure/DatabaseSafetyTest.php`, `phpunit.xml`, `docs/deploy/DATABASE_SAFETY_AND_BACKUP_RUNBOOK.md` | N/A | N/A | Validar comportamento em runtime com variáveis reais de ambiente e confirmar documentação de operação junto da equipa antes de qualquer reset local. |
| 2026-06-18 | Membros / Família | Sprint M2.4 — validação visual/técnica e preparação de produção do cutover de leitura concluídas sem alterações funcionais. Adicionados testes de superfície para ficha do membro (show/edit) e portal (perfil/família) com cenários de fallback e prova de ausência de escrita indireta nas novas tabelas; preparada documentação de deploy/rollback para futura entrada em produção. | `tests/Feature/Membros/MemberDataReadVisualSafetyTest.php`, `tests/Feature/Portal/PortalMemberDataReadSafetyTest.php`, `docs/deploy/MEMBERS_M2_4_VALIDATION_ROLLBACK_RUNBOOK.md`, `docs/PLANO_CONSOLIDACAO_MEMBROS.md`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 83% | 83% | `users` continua fonte operacional de escrita; sem alterações em store/update/import/relações familiares/Financeiro/Desportivo/migrations/frontend; sem deploy e sem comandos em produção. Próxima sprint recomendada: M2.5 (cutover de escrita). |
| 2026-06-18 | Membros / Família | Sprint M2.3 — cutover controlado de leitura com fallback. Criado `MemberDataReadService` com métodos `personalPayload()`, `configurationPayload()`, `mergedMemberPayload()`, `valueFromPersonal()` e `valueFromConfiguration()`. Aplicado em `MembrosController` (show/edit) e `PortalProfileController` (show). Leitura preferencial de `dados_pessoais`/`dados_configuracao` com fallback automático para `users`; `false` booleano preservado como valor válido. Sem cutover de escrita — `users` continua fonte operacional. 18 testes novos em `MemberDataReadFallbackTest`; 371 testes totais a verde. | `app/Services/Members/MemberDataReadService.php`, `app/Http/Controllers/MembrosController.php`, `app/Http/Controllers/PortalProfileController.php`, `tests/Feature/Membros/MemberDataReadFallbackTest.php`, `docs/PLANO_CONSOLIDACAO_MEMBROS.md`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 81% | 83% | `users` continua fonte operacional de escrita; produção não foi atualizada; sem alterações em store/update/import/relações familiares/Financeiro/Desportivo/migrations/models/frontend. M2.4 (validação/preparação) concluída; próxima sprint recomendada: M2.5 (cutover de escrita — dual-write ou escrita nas novas tabelas com fallback). |
| 2026-06-18 | Membros / Família | Sprint M2.2.1 — backfill real controlado dos dados do membro desbloqueado com guardas explícitas no comando (`--commit`, `--unlock-write`, `--confirm=BACKFILL_MEMBER_DATA`), mantendo dry-run por defeito. Adicionados `--chunk` e `--report-path`, bloqueio explícito de `--allow-updates` nesta sprint e execução real validada apenas em ambiente local/desenvolvimento, sem cutover de leitura/escrita em runtime. | `app/Console/Commands/MembersBackfillDataStructure.php`, `app/Services/Members/MemberDataMigrationService.php`, `tests/Feature/Membros/MemberDataBackfillCommandTest.php`, `docs/PLANO_CONSOLIDACAO_MEMBROS.md`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 80% | 81% | `users` continua fonte operacional; produção não foi atualizada; sem alterações em UI/controllers funcionais/imports, sem alterações em relações familiares e sem impacto em Financeiro/Desportivo. Próxima sprint recomendada: M2.3 (leitura com fallback) apenas após backfill auditado no ambiente alvo. |
| 2026-06-18 | Membros / Família | Sprint M2.2 — auditoria e simulação de backfill dos dados do membro concluídas sem escrita real. Criados comandos de auditoria (`members:audit-data-structure`) e simulação (`members:backfill-data-structure`) com opções `--user-id`, `--limit`, `--json`; criado serviço dedicado para mapeamento, normalização, assinatura de origem (`migration_source_hash`) e deteção de conflitos/paridade; `--commit` permanece bloqueado por segurança nesta sprint. | `app/Services/Members/MemberDataMigrationService.php`, `app/Console/Commands/MembersAuditDataStructure.php`, `app/Console/Commands/MembersBackfillDataStructure.php`, `bootstrap/app.php`, `tests/Feature/Membros/MemberDataBackfillCommandTest.php`, `docs/PLANO_CONSOLIDACAO_MEMBROS.md`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 79% | 80% | `users` continua fonte operacional; sem backfill real, sem switch de leitura/escrita, sem alterações em UI/controllers funcionais/imports, sem alterações em relações familiares e sem impacto em Financeiro/Desportivo. Próxima sprint recomendada: M2.3 (cutover de leitura com fallback) ou M2.2.1 (desbloqueio controlado de commit com relatório de paridade). |
| 2026-06-18 | Membros / Família | Sprint M2.1 — fundação estrutural dos dados do membro implementada com criação das tabelas `dados_pessoais` e `dados_configuracao`, models dedicados e relações `hasOne` em `User`, incluindo testes de estrutura/relações (unique por `user_id`, cascade delete e casts). Não houve cutover funcional: `users` mantém-se como fonte operacional nesta sprint. | `database/migrations/2026_06_18_120000_create_dados_pessoais_table.php`, `database/migrations/2026_06_18_120100_create_dados_configuracao_table.php`, `app/Models/DadosPessoais.php`, `app/Models/DadosConfiguracao.php`, `app/Models/User.php`, `tests/Feature/Membros/MemberDataStructureTest.php`, `docs/PLANO_CONSOLIDACAO_MEMBROS.md`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 78% | 79% | Sem backfill; sem alteração de leitura/escrita em controllers/UI/importação; sem alteração de relações familiares (`user_guardian`, `user_relationships`, `familias`/`familia_user`); sem alterações em Financeiro e Desportivo. Próxima sprint: M2.2 (auditoria/backfill em dry-run). |
| 2026-06-18 | Membros / Família | Sprint M2.0 — plano técnico de consolidação estrutural dos dados do membro concluído ao nível de auditoria, desenho e documentação. Foi realizado levantamento detalhado dos campos de `users`, validações, leituras/escritas em controllers/requests/frontend, dependências de importação e mapeamento de relações familiares (`user_guardian`, `user_relationships`, `familias`/`familia_user` e arrays legacy), com proposta faseada M2.1-M2.6, estratégia de compatibilidade, rollback e testes. Sem alterações funcionais nesta sprint. | `docs/PLANO_CONSOLIDACAO_MEMBROS.md`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md`, análise técnica em `app/Models/User.php`, `app/Http/Controllers/MembrosController.php`, `app/Http/Controllers/FamilyPortalController.php`, `app/Http/Controllers/PortalProfileController.php`, `app/Http/Controllers/PortalPageController.php`, `app/Http/Controllers/DashboardController.php`, `app/Services/Family/FamilyService.php`, `app/Services/Members/MemberImportService.php`, `database/migrations/0001_01_01_000000_create_users_table.php`, `database/migrations/2026_01_29_163055_add_spark_fields_to_users_table.php`, `database/migrations/2026_01_30_150000_extend_users_table_complete.php`, `database/migrations/2026_02_01_024147_create_user_relationships_table.php`, `database/migrations/2026_02_04_120000_create_user_guardian_pivot_table.php`, `database/migrations/2026_04_24_120000_create_familias_tables.php`, `resources/js/Pages/Membros/Show.tsx`, `resources/js/Components/Members/Tabs/PersonalTab.tsx`, `resources/js/Pages/Portal/Family.tsx` e testes Feature associados. | 78% | 78% | Sprint documental; não avançar para M2.1 nesta entrega. Sem migrations, sem alteração de fonte de verdade e sem mudanças funcionais em Financeiro/Desportivo. Pendem decisões de arquitetura para fonte canónica de relações familiares e papel definitivo de `user_relationships` na transição. |
| 2026-06-17 | Membros / Família | Sprint M1 — validação manual operacional concluída e fecho confirmado. Checklist validada no browser: lista de membros com pesquisa/filtros OK; estados vazios claros OK; separação funcional entre tipo de utilizador e perfil/permissões OK; ficha de membro abre sem quebra de tabs OK; tab administrativa `Família` visível OK; encarregado mostra educandos OK; atleta/educando mostra encarregado OK; membro sem relações mostra vazio claro OK; links `Abrir ficha` funcionam OK; sem duplicação visual de relações OK; utilizador sem permissão em modo consulta sem ações indevidas OK; `Dados Pessoais` mantém tipo de utilizador OK; `Configuração` mantém perfil/permissões e campos RGPD/transporte/afiliação/ficheiros OK; remoção de relações familiares/EE mantém confirmação prévia OK; Portal Família com mensagens operacionais para ausência de membros/permissões OK e apenas membros associados visíveis OK. Conclusão: M1 fechada operacionalmente. | Feedback manual no browser (2026-06-17); `resources/js/Pages/Membros/ListTab.tsx`, `resources/js/Pages/Membros/Show.tsx`, `resources/js/Components/Members/Tabs/FamilyTab.tsx`, `resources/js/Components/Members/Tabs/PersonalTab.tsx`, `resources/js/Components/Members/Tabs/ConfigurationTab.tsx`, `resources/js/Pages/Portal/Family.tsx`, `app/Http/Controllers/MembrosController.php`, `app/Http/Controllers/FamilyPortalController.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 77% | 78% | Sem migrations; sem alteração da fonte de verdade das relações familiares; módulo Financeiro sem alterações nesta validação; tab `Família` mantém-se consultiva; separação estrutural `users`/`dados_pessoais`/`dados_configuracao` e consolidação `user_guardian`+`user_relationships`+arrays legacy ficam para sprint futura; não avançar para M2 nesta alteração documental. |
| 2026-06-17 | Membros / Família | Sprint M1 — ajuste incremental: ficha administrativa de membro com nova tab `Família` em modo consultivo. A ficha passa a expor contexto familiar consolidado (`guardians`, `dependents`, famílias agregadas e contagens) para navegação entre membros sem alterar a fonte de verdade relacional nem criar novo fluxo de escrita. | `app/Http/Controllers/MembrosController.php`, `resources/js/Pages/Membros/Show.tsx`, `resources/js/Components/Members/Tabs/FamilyTab.tsx`, `tests/Feature/Membros/MembrosFamilyContextTabPayloadTest.php` | 76% | 77% | Mantém-se a pendência estrutural de convergência de relações (`user_guardian` + `user_relationships` + arrays legacy) para sprint dedicada; esta entrega é apenas de visibilidade/consulta. |
| 2026-06-17 | Membros / Família | Sprint M1 — hardening operacional do módulo Membros/Famílias/Encarregados de Educação (sem mudanças estruturais). Foram aplicadas melhorias de microcopy/estados vazios, clarificação funcional entre `Tipo de utilizador` (Dados Pessoais) e `Perfil/permissões` (Configuração), remoção de hardcoded de edição na ficha com uso de permissões reais já existentes, confirmações de remoção em relações EE/educando e mensagens operacionais no Portal Família quando não há gestão permitida ou membros visíveis. | `app/Http/Controllers/MembrosController.php`, `app/Http/Controllers/FamilyPortalController.php`, `resources/js/Pages/Membros/Show.tsx`, `resources/js/Pages/Membros/ListTab.tsx`, `resources/js/Components/Members/Tabs/PersonalTab.tsx`, `resources/js/Components/Members/Tabs/ConfigurationTab.tsx`, `resources/js/Pages/Portal/Family.tsx`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md` | 75% | 76% | Dívida estrutural mantida por decisão da sprint: `users` sobrecarregado, ausência de `dados_pessoais`/`dados_configuracao` dedicadas e coexistência `user_guardian` + `user_relationships` + arrays legacy. Requer sprint própria de consolidação. |
| 2026-05-19 | Global | Criação do ficheiro vivo de estado de desenvolvimento | Análise estática do repositório GitHub online | — | 60%–65% global | Executar validações runtime e atualizar por commit/PR. |
| 2026-05-20 | Financeiro | Sprint F1 — verdade financeira canónica tecnicamente concluída. Foram bloqueadas escritas diretas perigosas em faturas/mensalidades e movimentos; fluxos de liquidação/conciliação passam pelos serviços canónicos. | `FinanceiroController.php`, `ManualExpenseService.php`, `PaymentAllocationFlowTest.php` | 70% | 72% | Validação manual no browser ainda pendente. F2 deve corrigir bugs técnicos críticos e reforçar guardas fora da superfície HTTP principal. |
| 2026-05-20 | Financeiro | Sprint F1.1 — liquidação manual canónica com métodos de pagamento configuráveis. O backend passou a validar métodos ativos e a exigir linha bancária quando configurado; o frontend Financeiro e Configurações passaram a consumir a lista configurável. | `PaymentAllocationService.php`, `FinanceiroController.php`, `ConfiguracoesController.php`, `resources/js/Pages/Financeiro/FaturasTab.tsx`, `resources/js/Pages/Configuracoes/Index.tsx`, `tests/Feature/Financeiro/PaymentAllocationFlowTest.php`, `tests/Feature/Financeiro/FinanceDashboardFlowTest.php`, `tests/Feature/Configuracoes/PaymentMethodCrudTest.php` | 72% | 74% | Falta validação manual de UX no modal de pagamento e na gestão de métodos em Configurações. |
| 2026-05-20 | Financeiro | Sprint F1.2 — validação manual pós-correção do modal de pagamento. Confirmado no browser: transferência sem linha bancária bloqueia, transferência com linha bancária liquida/concilia, dinheiro sem linha bancária liquida, pedido fiscal pendente é criado e desconciliação reabre mensalidade/remover pedido pendente. | Feedback manual do utilizador em produção; modal Financeiro, tab Banco, tab Emissão Fiscal e Configurações > Financeiro | 74% | 76% | Avançar para F2: bugs técnicos críticos, incluindo delete seguro de faturas, `ilike`, validação de ZIP e limpeza de resquícios frontend. |
| 2026-05-20 | Financeiro | Sprint F2 — bugs técnicos críticos corrigidos no backend e frontend. Foram corrigidos os operadores de pesquisa dependentes do driver, a closure de sugestões bancárias com `$operator`, a validação condicional de ZIP, o delete inseguro de faturas, o tipo manual forçado a `mensalidade`, os lançamentos/stock otimistas falsos no frontend, o shadowing da rota de sugestões e o eager loading perigoso de movimentos. | `app/Http/Controllers/Controller.php`, `app/Http/Controllers/FinanceiroController.php`, `app/Http/Controllers/Financeiro/BankReconciliationSuggestionController.php`, `app/Http/Controllers/Financeiro/ReceiptImportController.php`, `app/Models/Movement.php`, `app/Services/Financeiro/BankReconciliationSuggestionService.php`, `resources/js/Pages/Financeiro/FaturasTab.tsx`, `routes/web.php`, `tests/Feature/Financeiro/FinanceiroCriticalBugFixesTest.php` | 76% | 79% | Tecnica e automaticamente concluida; falta validacao manual no browser para pesquisa bancária, delete bloqueado, faturas manuais por tipo e importacao sem ZIP. Nao avancar para F3 sem esse feedback. |
| 2026-05-21 | Financeiro | Sprint F2.1 — correção da validação manual da F2 no fluxo de faturas manuais. A liquidação deixou de depender de nº de recibo externo, o pagamento total cria pedido fiscal pendente, a emissão fiscal passa a retroalimentar `invoice.numero_recibo` e a reabertura de faturas pagas/parciais passou a usar endpoint canónico com reversão segura de pagamentos/alocações e do estado bancário antes de existir Wintouch. | `app/Http/Controllers/FinanceiroController.php`, `app/Services/Financeiro/PaymentAllocationService.php`, `app/Services/Financeiro/MonthlyInvoiceStatusService.php`, `app/Services/Financeiro/FiscalDocumentRequestService.php`, `resources/js/Pages/Financeiro/FaturasTab.tsx`, `routes/web.php`, `tests/Feature/Financeiro/PaymentAllocationFlowTest.php`, `tests/Feature/Financeiro/FiscalDocumentRequestFlowTest.php` | 79% | 80% | Validacao tecnica obrigatoria executada; F2 e F2.1 continuam pendentes de validacao manual orientada no browser. Nao avancar para F3 sem esse feedback. |
| 2026-05-21 | Financeiro | Sprint F2.2 — correção de UX/CSRF da tab Movimentos. Os requests de criação/edição/liquidação/apagar movimentos passaram a usar helper partilhado com `Accept: application/json`, `X-Requested-With`, `X-CSRF-TOKEN` e `credentials: same-origin`, com mensagem explícita para `419`; os dropdowns longos passaram a ter scroll e o CTA principal foi normalizado para `Novo Movimento`. Foram ainda adicionados testes HTTP para `POST /financeiro/movimentos` em JSON, multipart/FormData e validação obrigatória clara. | `resources/js/Pages/Financeiro/request.ts`, `resources/js/Pages/Financeiro/MovimentosTab.tsx`, `resources/js/Pages/Financeiro/FaturasTab.tsx`, `tests/Feature/Financeiro/ManualExpenseFlowsTest.php`, `tests/Feature/Financeiro/BankReconciliationSuggestionFlowTest.php` | 80% | 81% | `composer dump-autoload`, `php artisan migrate --pretend`, `php artisan test --filter=Financeiro` e `npm run build` executados. F2/F2.1/F2.2 continuam pendentes de validação manual orientada no browser; não avançar para F3 antes de repetir esses testes. |
| 2026-05-21 | Financeiro | Sprint F2.3 — remoção definitiva da dependência residual de nº de recibo no pagamento de faturas. A superfície administrativa de faturas deixou de transportar `numero_recibo`, o modal canónico foi renomeado para pagamento e os testes cobrem pagamento de faturas `material`, `inscricao` e `mensalidade` sem recibo, preservando `external_document_number` obrigatório apenas no `mark-issued` da Emissão Fiscal. | `resources/js/Pages/Financeiro/FaturasTab.tsx`, `app/Http/Requests/StoreInvoiceRequest.php`, `app/Http/Requests/UpdateInvoiceRequest.php`, `app/Http/Controllers/FinanceiroController.php`, `tests/Feature/Financeiro/PaymentAllocationFlowTest.php`, `tests/Feature/Financeiro/FiscalDocumentRequestFlowTest.php` | 81% | 82% | `composer dump-autoload`, `php artisan migrate --pretend`, `php artisan test --filter=PaymentAllocation`, `php artisan test --filter=FiscalDocumentRequestFlowTest`, `php artisan test --filter=Financeiro` e `npm run build` executados. Repetir validação manual orientada em browser para F2/F2.1/F2.2/F2.3; não avançar para F3 antes desse feedback. |
| 2026-05-22 | Financeiro | Sprint F2.3 — correção final do modal de pagamento em Faturas. O `Index` voltou a encaminhar o estado completo de faturas para a tab canónica, o modal passou a aceitar apenas métodos ativos, a mostrar seleção de linha bancária apenas quando o método o exige e a bloquear confirmação quando falta conciliação bancária obrigatória, sem regressão para o fluxo legado de movimentos nem reintrodução de nº de recibo. | `resources/js/Pages/Financeiro/Index.tsx`, `resources/js/Pages/Financeiro/FaturasTab.tsx`, `tests/Feature/Financeiro/FaturasTabFlowContractTest.php`, `tests/Feature/Financeiro/FinanceDashboardFlowTest.php`, `tests/Feature/Financeiro/PaymentAllocationFlowTest.php`, `tests/Feature/Financeiro/FiscalDocumentRequestFlowTest.php` | 82% | 83% | `composer dump-autoload`, `php artisan migrate --pretend`, `php artisan test --filter=FaturasTabFlowContractTest`, `php artisan test --filter=Financeiro`, `php artisan test --filter=PaymentAllocation`, `php artisan test --filter=FiscalDocument`, `php artisan test --filter=PaymentMethod` e `npm run build` executados. F2/F2.1/F2.2/F2.3 continuam pendentes de validação manual orientada no browser; não avançar para F3 antes desse feedback. |
| 2026-05-22 | Financeiro | Sprint F2.4 — correção limitada do popup `Liquidar Movimento` na tab Movimentos. O popup deixou de obrigar `numero_recibo`, passou a consumir apenas métodos ativos a partir dos `page props`, mostra seleção de linha bancária apenas para métodos com `requer_linha_bancaria`, bloqueia confirmação quando falta conciliação bancária obrigatória e envia `bank_statement_id` apenas quando aplicável. O endpoint `liquidarMovimento` passou a aceitar `numero_recibo` nulo e `bank_statement_id`, mantendo o motor financeiro canónico inalterado. | `resources/js/Pages/Financeiro/MovimentosTab.tsx`, `app/Http/Controllers/FinanceiroController.php`, `tests/Feature/Financeiro/ManualExpenseFlowsTest.php` | 83% | 84% | `php artisan test --filter=ManualExpenseFlowsTest`, `php artisan test --filter=PaymentAllocation`, `php artisan test --filter=Financeiro` e `npm run build` executados. F2/F2.1/F2.2/F2.3/F2.4 continuam pendentes de validação manual orientada no browser; não avançar para F3 antes desse feedback. |
| 2026-05-22 | Financeiro | Sprint F2.5 — reabertura controlada de movimentos liquidados. Movimentos `pago/parcial/pago_parcial` passaram a poder ser reabertos apenas pelo endpoint canónico, com reversão segura de alocações, pagamentos órfãos, reconciliação bancária e pedidos fiscais pendentes, bloqueando a operação quando já existe documento fiscal emitido ou nº externo associado. A tab Movimentos passou a expor ações explícitas de reabertura para `pendente` e `vencido`, sem reabrir o update direto. | `app/Services/Financeiro/FinancialSettlementService.php`, `app/Http/Controllers/FinanceiroController.php`, `routes/web.php`, `resources/js/Pages/Financeiro/MovimentosTab.tsx`, `tests/Feature/Financeiro/ManualExpenseFlowsTest.php` | 84% | 85% | `composer dump-autoload`, `php artisan migrate --pretend`, `php artisan test --filter=ManualExpenseFlowsTest`, `php artisan test --filter=PaymentAllocation`, `php artisan test --filter=Financeiro` e `npm run build` executados. F2/F2.1/F2.2/F2.3/F2.4/F2.5 continuam pendentes de validação manual orientada no browser; não avançar para F3 antes desse feedback. |
| 2026-05-22 | Financeiro | Validação manual em browser da sequência F2.4/F2.5 em Movimentos e da regressão crítica de Mensalidades. O utilizador confirmou que `Liquidar Movimento` já não obriga nº de recibo, filtra métodos inativos, permite `Dinheiro` sem linha bancária, mostra/bloqueia/permite `Transferência` conforme a linha bancária, que o movimento liquidado entra em Emissão Fiscal, que a reabertura para `pendente` e `vencido` funciona e que o pedido fiscal pendente é removido ao reabrir enquanto ainda não existe nº Wintouch. Confirmou também que, em Mensalidades, `Dinheiro` sem banco liquida sem nº de recibo, `Transferência` sem banco bloqueia, `Transferência` com banco liquida/concilia e a desconciliação no Banco reabre a mensalidade e remove o pedido fiscal pendente. | Feedback manual do utilizador no browser; Financeiro > Movimentos; Financeiro > Mensalidades; tab Banco; tab Emissão Fiscal | 85% | 85% | F2/F2.1/F2.2/F2.3/F2.4/F2.5 ficam fechadas operacionalmente na parte validada desta sequência; mantêm-se apenas pendências fora destas sprints. Não avançar para F3. |
| 2026-05-22 | Financeiro | Sprint F3.1 — leitura canónica de dívida e conta corrente. Foi criado `CurrentAccountService` para centralizar a leitura de faturas abertas, movimentos a receber, crédito disponível e saldo manual legado; o Dashboard atleta e o Portal Profile passaram a consumir `net_debt`/`manual_account_balance`, e `financeiro.invoices.open` passou a excluir faturas ocultas/futuras mantendo `valor_em_aberto` canónico em cenários com snapshots antigos. | `app/Services/Financeiro/CurrentAccountService.php`, `app/Http/Controllers/DashboardController.php`, `app/Http/Controllers/PortalProfileController.php`, `app/Http/Controllers/FinanceiroController.php`, `tests/Feature/Dashboard/DashboardEntryRoutingTest.php`, `tests/Feature/Portal/PortalProfileFamilyAccessTest.php`, `tests/Feature/Financeiro/PaymentAllocationFlowTest.php` | 85% | 86% | Validação técnica focada executada. Falta validação manual orientada em Dashboard atleta, Portal > Perfil e Financeiro > Faturas abertas para confirmar os novos valores no browser. |
| 2026-05-25 | Financeiro | Sprint F3.2 — alinhamento das superfícies de utilizador/família com `CurrentAccountService`. Ficha do membro e tabs locais deixaram de duplicar `conta_corrente_manual`, Portal Pagamentos passou a usar dívida líquida/valor em aberto real e Portal Família passou a somar apenas dívida aberta efetiva dos membros visíveis, separando crédito disponível e saldo manual legado. | `app/Http/Controllers/MembrosController.php`, `app/Http/Controllers/PortalPageController.php`, `app/Http/Controllers/FamilyPortalController.php`, `resources/js/Pages/Portal/Payments.tsx`, `resources/js/Pages/Portal/Family.tsx`, `resources/js/Components/Members/Tabs/FinancialTab.tsx`, `resources/js/Components/Members/Tabs/DashboardTab.tsx`, `tests/Feature/Membros/MembrosCurrentAccountSurfaceTest.php`, `tests/Feature/Portal/PortalPaymentsTest.php`, `tests/Feature/Portal/PortalFamilyCurrentAccountTest.php`, `tests/Feature/Dashboard/MemberDashboardSurfaceTest.php` | 86% | 87% | Tecnica e automaticamente concluida. Validacao manual orientada ainda pendente em Ficha do membro, Portal > Pagamentos e Portal > Familia. Nao avancar para F3.3 antes desse feedback. |
| 2026-05-25 | Financeiro | Sprint F3.2.1 — correção limitada das superfícies operacionais de conta corrente. Mantido `CurrentAccountService` como leitura canónica, mas removida a promoção operacional de `conta_corrente_manual`/`manual_account_balance`; tabs de membro e Portais Pagamentos/Família voltaram a usar linguagem funcional (`Conta Corrente`, `Mensalidades`, `Movimentos`, `Valor Pago`, `Em aberto`) e a indicar que novos ajustes devem ser feitos por movimento manual auditável no Financeiro. | `app/Http/Controllers/MembrosController.php`, `app/Http/Controllers/PortalPageController.php`, `app/Http/Controllers/FamilyPortalController.php`, `resources/js/Pages/Membros/Show.tsx`, `resources/js/Components/Members/Tabs/DashboardTab.tsx`, `resources/js/Components/Members/Tabs/FinancialTab.tsx`, `resources/js/Pages/Portal/Payments.tsx`, `resources/js/Pages/Portal/Family.tsx`, `tests/Feature/Membros/MembrosCurrentAccountSurfaceTest.php`, `tests/Feature/Financeiro/CurrentAccountServiceOperationalBalanceTest.php`, `tests/Feature/Dashboard/MemberCurrentAccountLanguageContractTest.php`, `tests/Feature/Portal/PortalCurrentAccountLanguageContractTest.php`, `tests/Feature/Portal/PortalPaymentsTest.php`, `tests/Feature/Portal/PortalFamilyCurrentAccountTest.php` | 87% | 87% | Validação técnica automática em curso; continua pendente validação manual orientada em Ficha do membro, Portal > Pagamentos e Portal > Família. Futura sprint pode migrar saldo manual legado para movimentos auditáveis. |
| 2026-05-26 | Financeiro | Sprint F3.3 — encerramento operacional de `conta_corrente_manual`. O campo deixou de ser editável em criação/edição de membro, passou a ser bloqueado por request com mensagem explícita, a importação de membros passou a ignorá-lo com aviso de compatibilidade, o Portal Profile deixou de o promover como `Conta corrente` e o Dashboard atleta deixou de o transportar no payload operacional. Ajustes de conta corrente passam exclusivamente por Movimentos manuais. | `app/Http/Requests/StoreMembroRequest.php`, `app/Http/Requests/UpdateMemberRequest.php`, `app/Http/Controllers/MembrosController.php`, `app/Http/Controllers/PortalProfileController.php`, `app/Http/Controllers/DashboardController.php`, `app/Services/Members/MemberImportService.php`, `resources/js/lib/member-import.ts`, `tests/Feature/Membros/MembrosCurrentAccountSurfaceTest.php`, `tests/Feature/Portal/PortalProfileFamilyAccessTest.php`, `tests/Feature/Financeiro/CurrentAccountServiceOperationalBalanceTest.php`, `tests/Feature/Dashboard/DashboardEntryRoutingTest.php` | 87% | 88% | Tecnica concluida; validacao manual orientada ainda pendente em Dashboard atleta, Portal > Perfil, Membros > Ficha e importacao real de membros. F3.4 fica apenas proposta para migracao controlada do legado para movimentos manuais. |
| 2026-05-26 | Financeiro | Sprint F3.4 — auditoria e preparação de migração controlada de `conta_corrente_manual` para Movimentos manuais. Foram criados um comando de auditoria e um comando de plano de migração em dry-run para listar membros afetados, totais positivos/negativos, movimentos manuais já existentes, pendências abertas e preview do movimento auditável futuro com origem `legacy_manual_current_account`. A validação operacional executada no servidor real com `php artisan finance:audit-manual-current-account` devolveu `0` membros afetados, `0.00` total positivo, `0.00` total negativo, `0.00` total líquido legado e estado semântico `no_legacy_manual_balance_found`, fechando a sprint sem necessidade de F3.5. | `app/Console/Commands/AuditManualCurrentAccount.php`, `app/Console/Commands/MigrateManualCurrentAccount.php`, `app/Console/Commands/Support/ManualCurrentAccountAuditReportBuilder.php`, `bootstrap/app.php`, `tests/Feature/Financeiro/ManualCurrentAccountCommandsTest.php`, `tests/Feature/Financeiro/CurrentAccountServiceOperationalBalanceTest.php`, `docs/PLANO_FECHO_MODULO_FINANCEIRO.md` | 88% | 88% | Validada operacionalmente em servidor real; nenhuma migração real executada; não avançar para F3.5. |
| 2026-06-01 | Financeiro | Correção limitada do fluxo manual de criação de extrato bancário na tab Banco. O submit passou a procurar CSRF primeiro em `meta[name="csrf-token"]` e depois em `XSRF-TOKEN` via cookie decodificado, bloqueando o POST com aviso claro quando não existe token; o POST para `financeiro.extratos.store` passou a tratar `419` com mensagem de sessão expirada e `422` com extração explícita de erro de validação, sem alterar regras de duplicados nem backend. | `resources/js/Pages/Financeiro/BancoTab.tsx` | 89% | 89% | Validar manualmente no browser a criação de extrato com sessão válida, sessão expirada e erro de validação para confirmar a UX do modal. |
| 2026-06-01 | Financeiro | Correção limitada F4.1.1 da UX de erro ao adicionar movimento bancário duplicado. O `handleAddExtrato` passou a mostrar de forma determinística a mensagem devolvida no `422` por ordem (`errors.extrato[0]`, primeiro erro de `errors`, `message`, fallback), mantendo o tratamento explícito de `419`, fallback claro para outros erros e o fecho/reset do modal apenas em sucesso. | `resources/js/Pages/Financeiro/BancoTab.tsx` | 89% | 89% | Validar manualmente no browser a criação de duplicado para confirmar que o toast exibe a mensagem de backend sem recorrer a DevTools. |
| 2026-06-01 | Financeiro | Correção limitada F4.1.2 para erro inline no modal Adicionar Movimento Bancário. Foi adicionado estado local `bankStatementFormError`, limpo na abertura do modal e no arranque do submit; validações locais e respostas `419`/`422`/outros erros agora além de `toast.error` preenchem a caixa visível no próprio modal, mantendo fecho e reset apenas em sucesso. | `resources/js/Pages/Financeiro/BancoTab.tsx` | 89% | 89% | Validar manualmente no browser criação de duplicado para confirmar mensagem de erro inline no modal sem depender de DevTools. |
| 2026-06-02 | Financeiro | Correção limitada F4.1.3 na importação de extrato XLS (bulk). O modal passou a tratar `422` com prioridade de mensagem (`message`, `errors.extratos[0]`, primeiro erro de `errors`, fallback), suportar múltiplas chaves de payload para linhas duplicadas/rejeitadas e contagem de rejeitadas, mostrar resumo inline visível com lista compacta limitada e manter ficheiro/preview/modal abertos em erro; `419` passou a mostrar mensagem explícita inline e toast. Sem alterações no backend nem nas regras de duplicados. | `resources/js/Pages/Financeiro/BancoTab.tsx` | 89% | 89% | `npm run build`, `php artisan test --filter=PaymentAllocation` e `php artisan test --filter=Financeiro` executados com sucesso; validação manual no browser ainda recomendada para confirmar o resumo com payload real de duplicados. |
| 2026-06-02 | Financeiro | Sprint F4.1 — validação manual no browser concluída e fecho operacional confirmado para canonicidade/guardas da conciliação bancária. Confirmados: criação de linha nova, bloqueio de duplicado individual com mensagem visível, importação bulk com rejeição de duplicados internos e de duplicados já existentes na BD com resumo, aceitação de linhas com mesma data/valor mas referência/descrição diferentes, conciliação manual de mensalidade com criação de pagamento/alocação e atualização de estado, desconciliação com reposição de estado e remoção de pedido fiscal pendente pré-Wintouch, criação/desconciliação de despesa com estados canónicos, fluxo legado/catalogar bloqueado e bloqueio de desconciliação quando já existe documento Wintouch emitido. | Feedback manual do utilizador no browser; `docs/ESTADO_VIVO_DESENVOLVIMENTO.md`; `docs/PLANO_FECHO_MODULO_FINANCEIRO.md` | 89% | 90% | F4.1 fechada operacionalmente. Não avançar para F4.2 nesta alteração documental. |
| 2026-06-02 | Financeiro | Sprint F4.2 — segurança de sugestões bancárias, rejeições e aliases tecnicamente concluída. A geração passou a respeitar rejeições auditáveis por assinatura de alocação, confirmações continuam a usar o fluxo canónico, sugestões rejeitadas deixam de reaparecer em regeneração normal e aliases aprendidos só reforçam score quando o alvo é claramente identificado. A UI passou a expor score, confiança, motivo principal e origem por alias/histórico com distinção visual entre confiança alta e baixa. | `app/Services/Financeiro/BankReconciliationSuggestionService.php`, `app/Services/Financeiro/ReconciliationAliasService.php`, `resources/js/Pages/Financeiro/BancoTab.tsx`, `tests/Feature/Financeiro/BankReconciliationSuggestionFlowTest.php` | 90% | 90% | Pendente validação manual no browser da rejeição, da regeneração forçada e da leitura visual de score/confiança. Não avançar para F4.3. |
| 2026-06-02 | Financeiro | Sprint F4.2.1 — sugestão bancária mensal cronológica por mês de referência tecnicamente concluída. O motor passou a inferir mês de referência a partir de descrição/referência (com fallback seguro para mês de movimento), construir sequência cronológica de mensalidades em aberto até esse mês sem ultrapassar `valor_por_conciliar`, diferenciar cobertura total/parcial com explicação explícita e evitar confiança alta quando a sequência é apenas por valor sem identidade segura. Mantido o veto por assinaturas rejeitadas e o fluxo canónico de confirmação. | `app/Services/Financeiro/BankReconciliationSuggestionService.php`, `tests/Feature/Financeiro/BankReconciliationSuggestionFlowTest.php`, `docs/PLANO_FECHO_MODULO_FINANCEIRO.md` | 90% | 91% | Validações automáticas executadas com sucesso (`composer dump-autoload`, `php artisan migrate --pretend`, `php artisan test --filter=BankReconciliationSuggestion`, `php artisan test --filter=PaymentAllocation`, `php artisan test --filter=Financeiro`, `npm run build`). Validação manual no browser ainda pendente antes de avançar para F4.3. |
| 2026-06-02 | Financeiro | Sprint F4.2.2 — prioridade da sequência mensal sobre sugestões isoladas por valor exato. O motor passou a penalizar variantes isoladas quando existe sequência cronológica mensal para o mesmo contexto, a persistir a sequência com metadata de referência mesmo abaixo do limiar padrão, e a manter cap de confiança quando falta identidade segura. | `app/Services/Financeiro/BankReconciliationSuggestionService.php`, `tests/Feature/Financeiro/BankReconciliationSuggestionFlowTest.php` | 91% | 91% | Regra validada em `BankReconciliationSuggestionFlowTest` e regressões gerais do módulo Financeiro; validação manual no browser ainda pendente. |
| 2026-06-02 | Financeiro | Sprint F4.2.3 — alocação assistida de transferência bancária por sugestão. O payload das sugestões passou a incluir `assisted_allocation_context` com referência mensal, lista de faturas e movimentos elegíveis, capacidade de crédito e defaults de alocação. O endpoint de confirmação de sugestão passou a aceitar payload customizado (`invoices`, `movements`, crédito explícito), com guardas de elegibilidade e liquidação delegada ao mesmo fluxo canónico usado na alocação manual (`FinancialSettlementService`). A UI da tab Banco passou a abrir o diálogo canónico de conciliação com contexto assistido pré-preenchido diretamente a partir da sugestão. | `app/Services/Financeiro/BankReconciliationSuggestionService.php`, `app/Http/Controllers/Financeiro/BankReconciliationSuggestionController.php`, `resources/js/Pages/Financeiro/BancoTab.tsx`, `resources/js/Pages/Financeiro/BankStatementReconciliationDialog.tsx`, `resources/js/Pages/Financeiro/types.ts`, `tests/Feature/Financeiro/BankReconciliationSuggestionFlowTest.php` | 91% | 92% | Executar validação manual no browser para o fluxo “Ajustar alocação assistida” na modal de sugestões e confirmar cenários de faturas+movimentos+crédito/remanescente. |
| 2026-06-17 | Financeiro | Sprint F4.2.3 — correção de interpretação funcional e fecho operacional. A sugestão deixou de funcionar como decisão fechada por uma única mensalidade e passou a abrir alocação assistida editável com destinos elegíveis até ao mês de referência (mensalidades/faturas), movimentos de receita em aberto e crédito remanescente. Validação manual no browser confirmada: abrir sugestão assistida abre alocação assistida (não confirmação fechada), cenário abril com €30 lista janeiro-abril e default só na mais antiga, cenário abril com €120 mantém as quatro e permite ajuste por linha, cenário €150 permite €120 mensalidades + €20 movimento + €10 crédito, bloqueios acima do valor da transferência/valor em aberto mantidos e estado final do extrato partial/reconciled correto. | `docs/ESTADO_VIVO_DESENVOLVIMENTO.md`, `docs/PLANO_FECHO_MODULO_FINANCEIRO.md` | 93% | 94% | F4.2.3 fechada operacionalmente. Não avançar para F4.3 nesta alteração documental. |
| 2026-06-17 | Financeiro | Sprint F4.3 — gestão visual de aliases bancários e rejeições em Configurações > Financeiro > Conciliação Bancária validada manualmente no browser e fechada operacionalmente. Confirmado: abertura da área OK; aliases com paginação server-side OK; pesquisa/filtros de aliases OK; alteração de tamanho de página em aliases OK; desativar alias OK; alias desativado deixa de influenciar novas sugestões OK; reativar alias OK; rejeições com paginação server-side OK; pesquisa/filtros de rejeições OK; limpar rejeição permite voltar a gerar sugestão OK; utilizador sem permissão não acede OK. A gestão fica visível e operacional sem alterar o motor canónico de pagamentos/alocação/desconciliação, importação XLS e conta corrente. | `docs/ESTADO_VIVO_DESENVOLVIMENTO.md`, `docs/PLANO_FECHO_MODULO_FINANCEIRO.md` | 95% | 96% | F4.3 fechada operacionalmente. Não avançar para F4.4 nesta alteração documental. |
| 2026-06-17 | Financeiro | Sprint F4.4 — relatórios e auditoria operacional da conciliação bancária validada manualmente no browser e fechada operacionalmente. Confirmado: abertura de `Configurações > Financeiro > Conciliação Bancária > Auditoria` OK; cards de resumo com e sem filtros OK; filtros por estado, intervalo de datas, pesquisa, método e com/sem crédito OK; ordenação por data e valor OK; paginação mantendo filtros OK; detalhe com allocations de mensalidades/faturas, movimentos, crédito criado, flags relevantes, histórico de desconciliação e estado fiscal quando disponível OK; linhas por conciliar sem allocations indevidas OK; perfil sem permissão sem acesso OK. O endpoint de leitura paginado (`GET /financeiro/banco/auditoria`) mantém filtros por estado/data/pesquisa/método/crédito/ordenação, resumo agregado e paginação server-side. Não houve alterações ao motor de pagamentos, alocação, desconciliação, importação XLS ou conta corrente. | `app/Http/Controllers/Financeiro/BankReconciliationAuditController.php`, `app/Services/Financeiro/BankReconciliationAuditService.php`, `routes/web.php`, `resources/js/Components/Configuracoes/Financeiro/BankReconciliationManagementTab.tsx`, `tests/Feature/Financeiro/BankReconciliationAuditEndpointTest.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md`, `docs/PLANO_FECHO_MODULO_FINANCEIRO.md` | 96% | 98% | F4.4 fechada operacionalmente. Não avançar para F4.5 nesta alteração documental. |
| 2026-06-17 | Financeiro | Sprint F4.5 — exportação e fecho operacional leve da conciliação bancária validada manualmente no browser e fechada operacionalmente. Foram adicionados endpoint de exportação detalhada (`GET /financeiro/banco/auditoria/export?format=csv`) e endpoint de resumo operacional (`GET /financeiro/banco/auditoria/export-summary`), ambos com os mesmos filtros da Auditoria e exportação de todas as linhas filtradas (ignorando paginação visual) até limite seguro. A subtab Auditoria passou a expor ações de exportação CSV, resumo CSV e indicador de conferência de período com base em `total_parcial` e `total_por_conciliar`, sem bloqueio transacional e sem mexer no motor financeiro canónico. XLSX permanece desativado por ausência de biblioteca segura já instalada. | `app/Http/Controllers/Financeiro/BankReconciliationAuditController.php`, `app/Services/Financeiro/BankReconciliationAuditService.php`, `routes/web.php`, `resources/js/Components/Configuracoes/Financeiro/BankReconciliationManagementTab.tsx`, `tests/Feature/Financeiro/BankReconciliationAuditEndpointTest.php`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md`, `docs/PLANO_FECHO_MODULO_FINANCEIRO.md` | 98% | 99% | Validada manualmente no browser e fechada operacionalmente. Não avançar para F4.6 nesta sprint. |
| 2026-06-17 | Financeiro | Sprint F4.6 — fecho final / hardening operacional tecnicamente concluído, sem funcionalidades grandes novas. Foi feita auditoria final de rotas/permissões F4, uniformização de linguagem operacional, reforço de confirmações para ações perigosas (criar despesa a partir de extrato e confirmar alocação assistida), melhoria de microcopy/estados de confiança e limpeza de detalhe técnico na Auditoria para foco operacional. Não houve alterações no motor financeiro canónico, nem em regras contabilísticas, nem em migrações. | `routes/web.php` (auditoria/permissões revista), `resources/js/Pages/Financeiro/BancoTab.tsx`, `resources/js/Pages/Financeiro/BankStatementReconciliationDialog.tsx`, `resources/js/Components/Configuracoes/Financeiro/BankReconciliationManagementTab.tsx`, `docs/ESTADO_VIVO_DESENVOLVIMENTO.md`, `docs/PLANO_FECHO_MODULO_FINANCEIRO.md` | 99% | 99% | Validada manualmente no browser em 2026-06-17 e fechada operacionalmente. Bloco F4 — Banco e conciliação bancária — fechado. Alterações foram de hardening, consistência UX, microcopy, confirmações e documentação. Motor financeiro canónico, regras contabilísticas e migrations sem alterações. Não avançar para F4.7. |
| 2026-06-02 | Financeiro | Sprint F4.1.4 — reorganização de navegação da Importação de Recibos: a tab `Importar Recibos` saiu do módulo Financeiro e passou para Configurações > Financeiro como subtab, reutilizando o mesmo componente e mantendo permissões (`admin` / `financeiro.importacao_recibos`) e endpoints atuais. | `resources/js/Pages/Financeiro/Index.tsx`, `resources/js/Pages/Configuracoes/Index.tsx`, `resources/js/Pages/Financeiro/ReceiptImportsTab.tsx`, `app/Http/Controllers/ConfiguracoesController.php`, `tests/Feature/Financeiro/FinanceiroReceiptImportTabLocationTest.php`, `tests/Feature/Configuracoes/ConfiguracoesReceiptImportTabTest.php` | 90% | 90% | Alteração apenas de localização UI. Sem mudanças em `ReceiptImportController`, `ReceiptCommitService`, `PaymentAllocationService`, `FinancialSettlementService`, `FiscalDocumentRequestService`, rotas/endpoints e regras financeiras/fiscais/conciliacão. |
| 2026-05-26 | Financeiro | Sprint F4.1 — implementação canónica de reconciliação bancária com guarda de duplicados. Novo modelo `BankStatement::duplicateSignatureFrom()` com normalização de dados (trim, round) para detecção robusta; `storeExtrato()` bloqueia criação individual duplicada; `storeExtratosBulk()` deteta duplicados intra-ficheiro e em BD, rejeitando com mensagens claras e retornando resumo (recebidas/criadas/rejeitadas); `conciliarExtrato()` bloqueado permanentemente com `abort(410)` direcionando para fluxo canónico; `unreconcile()` agora limpa `BankTransactionAllocation` confirmadas e restaura recibos ao estado `PENDING`; frontend `BancoTab.tsx` desabilitou `handleCatalogar` com aviso de descontinuação. | `app/Models/BankStatement.php` (assinatura/duplicados), `app/Models/BankTransactionAllocation.php` (status constants), `app/Http/Controllers/FinanceiroController.php` (guardas bulk), `app/Services/Financeiro/BankReconciliationService.php` (limpeza alocações), `resources/js/Pages/Financeiro/BancoTab.tsx` (desabilitação fluxo legado) | 88% | 89% | Validações técnicas executadas: `composer dump-autoload`, `php artisan test --filter=BankReconciliationSuggestionFlowTest` (33 passed), `npm run build` OK. Validação manual orientada ainda pendente: criar extrato duplicado (deve bloquear), bulk import com duplicados (deve rejeitar com resumo), desconciliação (deve limpar alocações). |

---

## 8. Como atualizar este documento no futuro

Sempre que for feita uma nova sprint, commit ou alteração relevante, atualizar:

1. a grelha da secção 4;
2. os riscos da secção 5;
3. as prioridades da secção 6;
4. o histórico da secção 7.

Formato recomendado para cada atualização:

```md
| AAAA-MM-DD | Módulo | O que mudou | Ficheiros/PR/commit | % antes | % depois | Pendências |
```

---

## 9. Nota metodológica

As percentagens deste documento são estimativas técnicas baseadas em leitura estática do código.

Para transformar estas percentagens em estado validado, é necessário executar:

```bash
composer dump-autoload
php artisan migrate --pretend
php artisan test
npm run build
```

Sempre que estes comandos forem executados com sucesso, deve ser acrescentada uma nota no histórico vivo.
